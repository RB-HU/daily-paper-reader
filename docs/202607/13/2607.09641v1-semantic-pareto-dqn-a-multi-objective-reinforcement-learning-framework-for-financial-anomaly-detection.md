---
title: "Semantic Pareto-DQN: A Multi-Objective Reinforcement Learning Framework for Financial Anomaly Detection"
title_zh: 语义帕累托-DQN：一种用于金融异常检测的多目标强化学习框架
authors: "Cláudio Lúcio do Val Lopes, Lucca Machado da Silva"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09641v1"
tags: ["query:fla"]
score: 9.0
evidence: 提出语义帕累托-DQN，一种用于金融异常检测的多目标RL智能体，直接是任务导向的金融智能体。
tldr: 金融异常检测中极度类别不平衡导致传统单目标算法陷入‘欺诈坍缩’，难以兼顾异常拦截与客户摩擦。为克服此问题，提出语义Pareto-DQN框架，通过将异构交易特征合成为自然语言叙述并由大语言模型编码，生成鲁棒的尺度不变状态表示。该框架采用向量化奖励解耦金融效能、操作摩擦与语义发现，动态追踪Pareto前沿以处理非对称成本。在电商欺诈与UCI信用数据集上成功消除零召回困境，以可控运营摩擦换取显著更高的少数类召回率，为金融风控提供新范式。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09641v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09641v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1250, \"height\": 271, \"label\": \"Table\"}]"
motivation: 金融异常检测的极端类别不平衡导致传统单目标算法陷入“欺诈坍缩”，无法有效平衡异常拦截与客户摩擦。
method: 将异构交易特征合成为自然语言叙述并由大语言模型编码，解耦金融效能、操作摩擦与语义发现，动态追踪连续Pareto前沿。
result: 在电商欺诈与UCI信用数据集上打破零召回陷阱，以可控摩擦实现了相比标量化基线更高的少数类召回率。
conclusion: 语义Pareto-DQN为金融异常检测提供了一种无需重采样即可平衡风险与体验的多目标强化学习范式。
---

## 摘要
金融异常检测面临极端的类别不平衡问题，导致传统单目标算法出现“欺诈塌缩”，默认偏向多数类，无法在异常拦截与客户摩擦之间取得平衡。为解决这一问题且避免畸变的数据重采样，我们提出了语义帕累托-DQN，一种多目标强化学习框架。该方法将异构交易特征合成为连贯的自然语言叙述，并由大型语言模型编码，从而产生鲁棒且尺度不变的状态表示。智能体优化一个向量化奖励，该奖励明确解耦了金融效能、操作摩擦和语义发现。通过映射连续的帕累托前沿，系统动态地权衡漏报异常与误报之间的不对称成本。在电子商务欺诈和UCI信用数据集上的实证评估表明，语义帕累托-DQN成功打破了零召回率陷阱。与标量化的基线方法相比，它在少数类召回率上表现优越，为以有限的操作摩擦换取金融异常发现提供了一种替代方案。

## Abstract
Financial anomaly detection suffers from extreme class imbalance, causing traditional single-objective algorithms to exhibit ``fraud collapse'', defaulting to the majority class and failing to balance anomaly interdiction with customer friction. To overcome this without distortive data resampling, we propose the Semantic Pareto-DQN, a multi-objective reinforcement learning framework. Our approach synthesizes heterogeneous transaction features into cohesive natural-language narratives, encoded by large language models, thereby producing a robust, scale-invariant state representation. The agent optimizes a vectorial reward that explicitly decouples financial efficacy, operational friction, and semantic discovery. By mapping the continuous Pareto frontier, the system dynamically navigates the asymmetric costs of missed anomalies versus false positives. Empirical evaluations across E-Commerce fraud and UCI Credit datasets show that semantic Pareto-DQN successfully shatters the zero-recall trap. It achieves superior minority-class recall compared to scalarized baselines, providing an alternative to trade bounded operational friction for financial anomaly discovery.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
金融异常检测（如信用卡欺诈、信贷违约）面临极端的类别不平衡，欺诈样本往往占比极低（<10%）。传统单目标算法（如标准监督学习或单目标强化学习）在优化单一标量指标（如对数似然、准确率）时，容易陷入“欺诈塌缩”：模型会退化为倾向预测多数类，导致召回率趋近于零，无法有效拦截异常。同时，真实业务要求动态平衡两个冲突目标：最大化异常拦截效能（减少漏报损失）与最小化客户摩擦（控制误报数量）。传统方法常通过数据重采样（如SMOTE）来缓解类别不平衡，但可能引入合成样本畸变决策边界。为此，该论文提出将异常检测建模为多目标序贯决策问题，利用多目标强化学习（MORL）直接优化向量化的奖励，在避免数据重采样的前提下，以有限的操作摩擦换取更高的少数类召回。

### 2. 论文提出的方法论：核心思想、关键技术细节
论文提出**语义帕累托-DQN（Semantic Pareto-DQN）**框架，核心思路是将异构交易特征转化为自然语言叙述，经语言模型编码为语义向量，构建状态表示；再通过多目标DQN同时优化三个解耦的奖励分量，动态追踪帕累托前沿。关键细节包括：

- **语义嵌入管线**：将原始特征（类别、数值、时序）合成为无目标文本叙述，利用`all-MiniLM-L6-v2` Sentence Transformer 编码为384维向量并做L₂归一化。状态由当前交易嵌入与滚动的操作摩擦（近期假阳性率FPR）拼接构成 `st = [vt || FPRt]`，以解决部分可观测问题。
- **多目标MDP与向量化奖励**：动作分为放行（0）或拦截（1）。奖励向量 `rt = [reff, rdrift, rdiv]` 包含三个解耦的目标：
  - **金融效能 `reff`**：根据交易金额、异常标签、滚动异常盛行率等，对真正例给予放大奖励，对漏报施加比例惩罚，对误报征收基础摩擦成本，对真负例给予微量收益。引入动态调整系数（λ、κ、δ）以平衡期望价值，防止代理在低异常时段退化为全部放行。
  - **操作摩擦制动 `rdrift`**：当局部假阳性率超过容忍阈值τ时，对误报施加二次惩罚，并设置裁剪上限M以防止早期探索造成梯度爆炸。
  - **语义多样性发现 `rdiv`**：基于当前交易嵌入与已拦截异常指数移动平均质心的余弦距离，鼓励发现与已知攻击模式语义差异大的新型异常。
- **网络结构**：采用帕累托-DQN，网络包含两个模块：
  - **奖励近似器**：一个3层MLP（512-256-128）预测即时奖励向量。
  - **连续帕累托曲面估计器**：将状态与采样的目标坐标（效能、摩擦）拼接，输出剩余目标（语义发现）的最大可达成值，以估计帕累托前沿。
- **动作选择**：利用超体积指标（Hypervolume）评估每个动作对应的期望回报集合，选择最大化超体积的动作，从而在不预先标量化的前提下导航帕累托前沿。

### 3. 实验设计：数据集、基准与对比方法
实验在两个金融数据集上进行，均按时间顺序划分训练（80%）和测试（20%），以保证序贯因果性并无数据泄露。

- **电子商务欺诈数据集**：约15万笔交易，欺诈样本14151个（<10%），包含原始交易特征及工程特征。状态通过将交易合成为自然语言叙述后嵌入得到。
- **UCI信用卡客户违约数据集**：包含23个变量（静态人口统计+6个月动态还款记录），用于预测二元违约。状态通过叙述合成和嵌入获得，信用额度做了对数变换以稳定奖励空间。

每个数据集上对比以下方法：
- XGBoost (原始表格特征)：使用频率编码和工程特征的梯度提升机，优化对数损失。
- XGBoost (语义嵌入)：仅使用语义向量作为特征训练XGBoost。
- 标准DQN；基于语义向量，优化标量化奖励的单目标RL。
- 本文提出的语义帕累托-DQN。

评估指标包括混淆矩阵计数、精确率、召回率、F1分数。

### 4. 资源与算力
论文未明确指出所使用的GPU型号、数量或训练时长等具体算力资源。未提及模型训练的硬件环境或计算开销。

### 5. 实验数量与充分性
- 实验组数：两个数据集，每个数据集四个模型对比，共8组核心实验（结果见表1、表2）。未在正文中报告多重随机种子的交叉验证或方差区间，仅提供单次确定性划分的点评估。文中虽讨论了消融分析思路（移除`rdiv`奖励分量、移除LLM叙述编码），但并未实际执行对应的消融实验，只是定性分析各组件的作用。
- 实验公正性：对比了静态监督模型与动态强化学习模型，以及单目标与多目标变体，设计较为公平。但DQN等RL方法未提供与更多MORL基线（如标量化帕累托方法）的对比，且缺少超参数敏感性分析。部分关键超参数（λ, κ, δ, η, τ, M）依赖离线手动校准，未探讨这些参数对性能的影响。总体上，实验数量偏少，对框架的鲁棒性和普适性验证不够充分，但已展示了基本优势。

### 6. 论文的主要结论与发现
- 语义帕累托-DQN成功打破了传统方法在极端不平衡下的零召回率陷阱。在电子商务数据上，召回率从静态模型的0.000（XGBoost原始表格）提升至0.267；在信用违约数据上，召回率从0.353提升至0.419，且F1分数均更高。
- 单目标方法（包括带有语义嵌入的XGBoost和标准DQN）因追求全局误差最小化，严重偏向多数类，而帕累托-DQN通过解耦奖励并在超体积指标下选择动作，实现了召回率与精确率的主动权衡，允许在可控摩擦下提升异常发现。
- 语义多样性奖励促使代理不仅拦截高密度历史欺诈，还能捕捉语义距离远的新颖攻击模式。
- LLM叙述编码是计算语义多样性的几何基础，但用在静态树模型中会导致性能下降（语义空间密集且难以划定尖锐决策边界），而多目标DQN则能充分利用该连续流形。

### 7. 优点
- **创新性问题建模**：将金融异常检测建模为语义多目标MDP，并引入解耦的效能、摩擦与语义多样性三目标，避免了重采样带来的畸变。
- **语义嵌入的应用**：利用大模型叙述编码从异构特征中自动提取上下文信息，且为多样性奖励提供可计算的几何空间。
- **帕累托前沿导航**：通过超体积指标和连续帕累托曲面估计实现无先验标量化的多目标权衡，能动态适应业务需求。
- **实证显著效果**：在两个不同特性的数据集上均大幅提升少数类召回，论证了框架的有效性。

### 8. 不足与局限
- **算力与实验可复现性信息缺失**：未提供模型训练所用硬件、训练时长，未报告多种子结果的均值和方差，仅给出单次确定性结果，可靠性受限。
- **实验覆盖不够充分**：缺少消融实验的实际数据，未对比其他多目标RL算法，也未进行超参数敏感性分析或统计显著性检验。
- **超参数依赖离线校准**：多个缩放系数（λ, κ, δ, η, τ, M）需基于数据集特性和离线统计手工设定，缺乏自动化或自适应机制，泛化到新领域时需要大量专家调参。
- **参考点固化的局限**：超体积指标依赖固定的下界参考点，一旦业务风险偏好变化后，无法在不重新训练的情况下动态调整目标偏好。
- **语义嵌入的双刃剑效应**：尽管对多目标RL有利，但在单目标或简单基线中可能导致性能退化，说明该管线对模型结构有特定依赖。
- **应用限制**：现阶段只验证了二元分类（放行/拦截），对更复杂的多动作场景（如阶梯式审查）未做探索；且叙事生成的质量可能影响状态表示稳定性，论文未讨论文本生成的控制细节。

（完）
