---
title: Strategy-Aware Parameter-Efficient Adaptation for LLM-based Auto-Bidding
title_zh: 面向LLM自动出价的策略感知参数高效适配
authors: "Songyue Cai, Lianyu Wang, Shan Gu, Ziru Xu, Jian Xu, Xiaofeng Zhu, Bo Zheng"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24232v1"
tags: ["query:fla"]
score: 9.0
evidence: 面向出价场景的LLM代理参数高效微调
tldr: "广告自动出价面临轨迹与文本交互浅层、微调成本高昂等挑战。SAGE提出策略感知的参数高效适配框架，通过位置增强、文本对齐和约束门控LoRA三模块实现多模态融合与高效适配。在大规模基准测试中，SAGE仅需全量微调不到10%的可训练参数即取得一致优越性能。该工作为LLM在受限自动出价任务中利用预训练知识提供了轻量化和有效性兼具的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24232v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1826, \"height\": 1166, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24232v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 874, \"height\": 357, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24232v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1558, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 880, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 188, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 877, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 878, \"height\": 213, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 880, \"height\": 438, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 615, \"height\": 468, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24232v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 790, \"height\": 290, \"label\": \"Table\"}]"
motivation: 现有LLM自动出价方法存在轨迹-文本交互浅层且需昂贵微调，限制了预训练知识在多样约束下的高效利用。
method: 提出SAGE框架，包含时序语义位置嵌入、门控交叉注意力文本对齐和约束门控LoRA模块，实现轻量级多模态对齐和策略适配。
result: "在大规模自动出价基准上，SAGE以不到10%的全微调参数量取得最优性能，消融实验验证各模块的关键作用。"
conclusion: 参数高效的SAGE框架有效融合轨迹与文本模态，在维持高性能的同时大幅降低计算开销，为LLM驱动的自动出价提供了实用路径。
---

## 摘要
广告出价已从手动策略演进到能更好地适应大规模动态拍卖环境的自动出价系统。尽管大语言模型（LLM）的最新进展为自动出价提供了强大的推理能力，但现有方法存在轨迹-文本交互浅层化、微调成本高昂等问题，阻碍了在多样化约束下对预训练知识的高效利用。针对这些挑战，我们提出SAGE——一种由LLM引导的策略感知型自动出价框架，以实现高效出价。SAGE为带有约束的LLM自动出价引入了一种参数高效的多模态对齐框架。具体而言，SAGE包含三个关键组件：（i）位置增强模块采用时序-语义位置嵌入，有效捕获内在动态与语义结构；（ii）文本对齐模块利用门控交叉注意力对齐轨迹与文本模态的嵌入空间，实现有效的多模态融合，同时缓解长轨迹带来的计算开销；（iii）约束门控LoRA模块以约束作为路由信号，仅激活一小部分专家，从而高效地调整冻结LLM的行为。在大规模自动出价基准上的大量实验表明，SAGE在调整不到全量微调所需可训练参数10%的情况下，持续取得优越性能。消融研究进一步验证了每个组件对框架整体性能的关键贡献。

## Abstract
Advertising bidding has evolved from manual strategies to auto-bidding systems better adapted for large-scale, dynamic auction environments. While recent advances in Large Language Models (LLMs) offer strong reasoning for auto-bidding, existing methods suffer from shallow trajectory-text interactions and require costly fine-tuning, hindering the efficient use of pretrained knowledge under diverse constraints. To address these challenges, we propose SAGE, a novel Strategy-aware Auto-bidding framework Guided by LLMs for Efficient bidding. SAGE introduces a parameter-efficient multi-modal alignment framework for constrained auto-bidding with LLMs. Specifically, SAGE comprises three key components: (i) the position augmentation module adopts temporal-semantic positional embeddings to effectively capture the intrinsic dynamics and semantic structures; (ii) the text alignment module leverages gated cross-attention to align the embedding spaces of trajectory and text modalities, enabling effective multi-modal fusion while alleviating the computational overhead caused by long trajectories; (iii) the constraint-gated LoRA module employs constraints as routing signals, activating only a small subset of experts to adapt the behavior of a frozen LLM efficiently. Extensive experiments on large-scale auto-bidding benchmark demonstrate that SAGE consistently achieves superior performance while tuning less than 10% of the trainable parameters required by full fine-tuning. Ablation studies further validate the critical contribution of each component to the framework's overall performance.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将对这篇名为《面向LLM自动出价的策略感知参数高效适配》的论文进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

- **研究背景**：
  - 在线广告出价已从手动策略演变为自动出价系统，以应对大规模、动态的拍卖环境。
  - 大语言模型（LLM）因其强大的推理能力，被引入自动出价任务，可处理多模态信息（如数值轨迹和文本约束）。

- **核心问题**：
  - 现有基于LLM的自动出价方法存在三大关键缺陷，限制了预训练知识在多样化约束下的高效利用：
    1.  **轨迹表征浅层**：对出价序列中不同语义的令牌（如状态、动作、回报）区分不足，仅依赖简单的可学习位置嵌入，忽略内部结构。
    2.  **多模态融合低效**：简单拼接轨迹与文本嵌入，忽略模态间嵌入空间差异，导致跨模态交互不充分且增加输入长度和计算开销。
    3.  **模型适配成本高昂**：通常采用全量微调适配LLM，需更新海量参数，计算和部署成本高，且可能破坏预训练先验知识，迁移性差。

- **整体含义**：
  - 本文旨在提出一种 **策略感知（Strategy-Aware）且参数高效（Parameter-Efficient）** 的新型自动出价框架，以系统性地解决上述挑战，实现更优决策性能的同时，大幅降低计算资源消耗。

### 2. 论文提出的方法论

SAGE框架的核心思想是解耦并优化轨迹表征、多模态融合和模型适配这三个环节。其方法流程如下：

1.  **输入处理**：
    - **轨迹模态**：一条出价轨迹由多个时间步的回报（RTG）、状态（S）、动作（A）令牌序列构成。
    - **文本模态**：分为“任务描述提示`p_task`”和一个独立于前者的“条件约束提示 `p_con`”。

2.  **核心组件**：
    - **A. 位置增强（Position Augmentation）**：
      - **目标**：解决轨迹表征浅层的问题。
      - **实现**：将位置嵌入分解为两部分，对每个令牌进行增强。
        - **语义嵌入**：引入少量可学习参数 `pos_s`，为同一语义类型（RTG, S, A）的令牌在不同时间步共享相同的语义嵌入，增强模型对令牌功能角色的区分能力。
        - **时序嵌入**：采用非参数的**正弦位置编码 `pos'`** 作为时序嵌入，处理不同时间步的位置信息，使其能很好地泛化到变长轨迹。
      - **公式化表示**：`e_t,k = e_t,k + pos'_t + pos_s_k`，其中 `e_t,k` 为 `t` 时刻、`k` 类型令牌的原始嵌入。

    - **B. 文本对齐（Text Alignment）**：
      - **目标**：解决多模态融合低效的问题。
      - **实现**：采用**门控交叉注意力**（Gated Cross-Attention）机制，在输入LLM前融合轨迹与文本信息。
        - 以轨迹嵌入 `e` 作为**查询（Query）**，以任务描述文本嵌入 `e_task` 作为**键（Key）和值（Value）**。
        - 通过交叉注意力，轨迹动态地从文本描述中提取相关语义信息，增强自身表征。
        - 门控机制允许模型控制交叉模态特征的保留比例。
      - **优点**：既实现了嵌入空间的对齐，又因为输出仅保留轨迹长度 `L`，而显著压缩了输入LLM的序列长度（避免了之前 `N_task + L` 的长序列）。

    - **C. 约束门控LoRA（Constraint-Gated LoRA）**：
      - **目标**：解决模型适配成本高且无法感知策略变化的问题。
      - **实现**：将约束条件作为路由信号，驱动参数的**高效适配**。
        - **条件信息提取**：在LLM的每一层，通过另一个**门控交叉注意力**，让该层的隐藏状态 `H_l`（作查询）从条件约束文本嵌入 `e_con`（作键和值）中提取约束信息 `C_l`。
        - **生成门控信号**：将同一时间步的不同令牌（R, S, A）对应的约束信息，通过多层感知机MLP聚合，生成该时间步的门控信号 `y_l_t`。
        - **专家混合LoRA（MoE-LoRA）**：设计一个包含多个LoRA专家的模块。门控信号 `y_l_t` 通过一个软路由网络选择并激活一个或多个专家。
        - **参数高效适配**：对冻结的LLM骨干网络，其权重矩阵 `W` 的输出，加上被选中专家所代表的低秩更新 `α/r * H_l_t * A_m ⊤ B_m`，从而以极少的可训练参数实现策略自适应。

3.  **最终输出与优化**：
    - 将约束门控LoRA的输出，经过一个线性层预测当前时间步的动作 `a_t`。
    - 使用均方误差（MSE）损失进行优化。

### 3. 实验设计

- **数据集**：
  - 采用阿里巴巴构建的大规模公开自动出价基准 **AuctionNet**，包含两个变体：
    - **AuctionNet-dense**：转化密集版本。
    - **AuctionNet-sparse**：转化稀疏版本。
  - 每个数据集包含48个广告主，超过500,000个曝光机会。

- **评估指标**：
  - **转化量**：无约束下的总转化次数。
  - **分数**：在CPA（单次行动成本）约束下的综合得分，通过一个惩罚项（与目标CPA的偏差）对转化量进行折损。

- **对比方法**：
  - **传统方法**：包括4种离线强化学习方法（USCB, BCQ, CQL, IQL）、1种基于规划的生成式出价方法（DiffBid）、3种序列建模方法（DT, CDT, DT-score）。
  - **LLM基础方法**：包括5种LLM方法（Prompting, SFT, GRPO, LLM-DT, Prompt-LLM-DT），覆盖了纯语言模态和数值序列模态。

### 4. 资源与算力

- **文中明确说明**：
  - 骨干模型为 **Qwen2.5-Instruct-0.5B**。
  - 训练设置：批量大小128，学习率1e-5，优化器AdamW，训练步数400,000。
- **未明确说明**：
  - 论文**没有提供**所使用GPU的型号、数量以及具体的训练总时长。

### 5. 实验数量与充分性

- **实验组数**：
  1.  **主要结果对比**：在两种数据集（AuctionNet-dense/sparse）和五个不同预算规模（50%-150%）下，与多达14种基线方法对比，并分别与传统方法和LLM方法进行分组比较。
  2.  **消融研究**：
      - 针对位置增强和文本对齐模块的消融。
      - 针对位置嵌入中语义/时序/传统嵌入的细分消融。
      - 针对约束门控LoRA模块，从是否使用LoRA、是否引入约束信息、是否进行全量微调等多个维度进行细粒度消融。
  3.  **可扩展性分析**：在Qwen2.5-Instruct的0.5B、1.5B、3B三个不同规模的骨干模型上进行了对比。
  4.  **超参数敏感性分析**：对MoE专家数量和LoRA秩的影响进行了分析。
  5.  **专家负载均衡分析**：对MoE中不同专家的利用率进行了统计分析。

- **充分性与客观性评估**：
  - **高度充分**：实验设计非常详尽和系统。消融实验设计精巧，从模块级到组件级，再到与完全替代方案的对比，有力地支撑了各个组件设计的有效性。多数据、多基线、多预算的设定保证了结论的稳健性。
  - **客观公平**：对比基准广泛，覆盖了多个流派的最优方法。所有方法在同一公开基准上比较，具有公平性。

### 6. 论文的主要结论与发现

- SAGA框架在所有数据集和预算设定下，均取得了**最优性能**。
- 相比最好的传统方法（DT-score），SAGE在密集和稀疏数据集上分别提升了**4.49%** 和 **7.83%**。
- 相比最好的LLM基线（LLM-DT），SAGE在两个数据集上分别提升了**6.40%** 和 **10.15%**，且在稀疏数据上优势更明显。
- SAGE在实现性能领先的同时，仅需全量微调约**8.30%** 的可训练参数，证明了其高效性。
- 消融实验证实，位置增强、文本对齐和约束门控LoRA三个模块均对最终性能有**关键且互相协同的贡献**。
- 冻结骨干网络通常优于全量微调，因为全量微调可能会破坏LLM宝贵的预训练知识。

### 7. 优点

- **问题洞察深刻**：准确地将现有LLM出价方法的不足归纳为表征、融合、适配三个层面的系统性问题，逻辑清晰。
- **方法设计优雅且高效**：
  - 模块化设计，每个模块针对一个特定挑战，架构清晰。
  - 语义与时序解耦的位置增强，以极少参数提升了表征能力。
  - 交叉注意力用于多模态融合和对齐，同时作为输入压缩的巧妙手段，一举两得。
  - 将广告约束作为路由信号驱动MoE-LoRA，实现了从“通用的参数高效”到“策略感知的参数高效”的升华。
- **实验验证极为扎实**：多维度、细粒度的实验设计，特别是通过交叉组合的消融实验，无可辩驳地证明了每个模块和设计选择的有效性，为后续研究提供了极具参考价值的范式。

### 8. 不足与局限

- **算力与资源细节缺失**：未报告GPU型号、数量和训练时长，使得其他研究者难以精确评估和复现该方法的实际计算成本（尽管参数量减少了，但引入的交叉注意力和MoE路由可能增加部分计算时延）。
- **对LLM特定能力的依赖度未知**：虽然证明了小模型的有效性，但SAGE框架的性能提升是否严重依赖于骨干LLM的通用推理能力（该能力是预训练得来的），文章中并未设计实验进行剥离（例如，与随机初始化的同结构Transformer对比）。
- **仿真与现实的差距**：所有实验均在静态离线数据集上进行。在线的、动态变化的真实竞价环境中，模型是否能保持其参数高效和策略感知的优势，尚需实践检验。
- **提示工程的敏感性**：框架有效性与文中精心设计的“任务描述”和“条件约束”提示模板高度相关，未见对不同提示词风格或复杂度的鲁棒性分析。

（完）
