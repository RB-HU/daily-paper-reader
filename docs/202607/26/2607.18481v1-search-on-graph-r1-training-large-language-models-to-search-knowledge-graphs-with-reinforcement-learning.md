---
title: "Search-on-Graph-R1: Training Large Language Models to Search Knowledge Graphs with Reinforcement Learning"
title_zh: Search-on-Graph-R1：通过强化学习训练大语言模型搜索知识图谱
authors: "Jia Ao Sun, Hao Yu, Fengran Mo, Zhan Su, Yuchen Hui, Bang Liu, Jian-Yun Nie"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18481v1"
tags: ["query:fla"]
score: 8.0
evidence: 通过监督微调和强化学习训练精简大模型自主导航知识图谱
tldr: 知识图谱问答需从主题实体多跳导航至答案，现有方法依赖大型LLM进行图谱探索，导致部署成本高昂。Search-on-Graph-R1提出通过监督微调与强化学习，将搜索导航能力内化到8B小模型中，训练时借助黄金SPARQL查询使教师模型沿已知答案路径使用Search工具，生成接地于真实知识图谱的轨迹。在WebQSP、CWQ和GrailQA三个数据集上，8B参数的Search-on-Graph-R1模型中，其性能全面超越所有冻结的前沿LLM系统，在CWQ上取得了我们所比较的所有系统中最强的结果，同时无需任何推理辅助模块和训练阶段的LLM评判。进一步分析表明，SFT与RL贡献了互补的增益，该方法能够跨模型家族迁移，RL学习到比其SFT初始化更少的Search调用次数即可达到答案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18481v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1650, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18481v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 572, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18481v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 812, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 604, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1381, \"height\": 1059, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 808, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 808, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 802, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1042, \"height\": 730, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18481v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 735, \"height\": 551, \"label\": \"Table\"}]"
motivation: 现有知识图谱问答依赖大型LLM进行多跳搜索，部署成本高昂，亟需将导航能力压缩到小模型中以实现高效部署。
method: 先使用黄金SPARQL查询引导教师模型在真实知识图谱上生成搜索轨迹，然后通过SFT和RL分阶段训练8B模型，将搜索能力内化。
result: 在WebQSP、CWQ和GrailQA上，8B模型性能超越所有冻结前沿LLM，取得CWQ最佳结果，RL学习到更少搜索步骤即可到达答案。
conclusion: SFT与RL带来互补增益，该方法可跨模型家族迁移，为低成本部署高效知识图谱问答提供了新范式。
---

## 摘要
知识图谱问答（KGQA）需要从主题实体导航到相隔若干关系的答案。近期方法提示前沿大语言模型通过检索工具探索图谱，但它们依赖于大规模推理，部署成本高昂。我们提出 Search-on-Graph-R1，通过监督微调（SFT）和随后的强化学习（RL），将这种导航能力内化到一个紧凑的 8B 模型中。我们的核心思想是使用每个问题的黄金 SPARQL 查询为前沿教师模型提供支撑，使教师借助实时的 Search 工具遍历已知的答案承载路径，而无需自行发现该路径。由于每次调用都针对实时 Freebase 服务器执行，因此生成的轨迹自然扎根于知识图谱。在 WebQSP、CWQ 和 GrailQA 上，8B 的 Search-on-Graph-R1 超越了所有对比的冻结前沿大语言模型系统，并在 CWQ 上取得了对比中所有系统的最佳结果。它在推理时无需辅助模块，训练时也无需大语言模型评判。分离每个训练阶段表明，SFT 和 RL 贡献了互补的增益，我们的方法可以跨模型系列迁移，并且 RL 学会使用比 SFT 初始化更少的 Search 调用次数到达答案。

## Abstract
Knowledge graph question answering (KGQA) requires navigating from topic entities to an answer several relations away. Recent methods prompt a frontier LLM to explore the graph through a retrieval tool, but their reliance on frontier-scale inference makes them costly to deploy. We present Search-on-Graph-R1 (\sogrone{}), which internalizes this navigation into a compact 8B model through supervised fine-tuning (SFT) followed by reinforcement learning (RL). Our central idea is to scaffold a frontier teacher with each question's gold SPARQL query, so the teacher traverses a known answer-bearing path with a live \texttt{Search} tool rather than having to discover the path itself. Since every call executes against a live Freebase server, the resulting trajectories are grounded in the knowledge graph by construction. On WebQSP, CWQ, and GrailQA, \sogrone{} at 8B surpasses every frozen frontier-LLM system in our comparison and posts the strongest results on CWQ of any system we compare against. It does so using no auxiliary module at inference and no LLM judge during training. Isolating each training stage shows that SFT and RL contribute complementary gains, our approach transfers across model families, and RL learns to reach answers in fewer \texttt{Search} calls than its SFT initialization.

---

## 论文详细总结（自动生成）

好的，我将根据你提供的论文内容，以资深学术论文分析助手的身份，对其进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：如何将知识图谱问答（KGQA）中复杂的多跳推理与实时知识图谱（KG）导航能力，从参数规模庞大、推理成本高昂的前沿大语言模型（LLM），有效迁移并内化到一个紧凑、可部署的小模型（如8B参数）中。
*   **整体含义**：论文旨在解决当前KGQA系统面临的实际部署瓶颈。现有最佳方法依赖`GPT-4`等大型模型通过工具调用进行实时探索，导致高延迟、高成本和高访问门槛。另一方面，语义解析方法虽可使用小模型，但泛化能力受限。`Search-on-Graph-R1`旨在结合两者优点，通过“监督微调 + 强化学习”的方式，训练一个小模型学会自主、高效地在图谱中导航，从而在精度上匹敌甚至超越大型模型系统，同时大幅降低推理成本。

### 2. 论文提出的方法论

核心思想是通过一个三阶段的训练流程，将知识图谱导航策略“蒸馏”并优化到一个紧凑模型中。

*   **阶段1：SPARQL-支撑的冷启动轨迹生成**
    *   **核心思想**：利用KGQA数据集中自带的、但模型在推理时不可见的黄金`SPARQL`查询，作为前沿教师模型（Qwen3-235B）的导航蓝图。
    *   **实现流程**：
        1.  将问题、主题实体和黄金`SPARQL`查询提供给教师模型。
        2.  教师模型解读`SPARQL`，将其转化为一系列对实时Freebase图谱的`Search`工具调用。
        3.  `Search`工具返回图谱中的邻接三元组，构成观察。
        4.  整个“工具调用-观察-最终答案”的序列，形成一条扎根于真实图谱的多轮对话轨迹。这些轨迹是学生模型最初的模仿学习材料。

*   **阶段2：监督微调**
    *   **核心思想**：让紧凑型学生模型（Llama-3.1-8B-Instruct）模仿教师模型生成的、正确的导航行为。
    *   **实现细节**：使用LoRA（低秩适应）技术，在学生模型上微调。损失函数仅计算在“助手”轮次（即工具调用和最终答案部分），使其学会何时调用工具以及如何给出答案，但不预测工具的输出。

*   **阶段3：强化学习**
    *   **核心思想**：在模仿学习的基础上，通过试错探索和自我优化，进一步提升模型的导航效率和准确性。
    *   **算法**：采用组相对策略优化（GRPO），学生模型与实时KG环境交互，产生完整轨迹。
    *   **奖励函数设计**：
        *   `r(τ) = g(τ) * r_em(τ) * f(nt)`
        *   `g(τ)`：格式门函数，强制模型至少调用一次工具，并以干净的`Final answer: `格式结束，过滤无效轨迹。
        *   `r_em(τ)`：二元精确匹配奖励，判断预测答案集合是否与标准答案完全一致，提供稀疏但明确的正确性信号。
        *   `f(nt)`：一个基于`nt`（工具调用次数）的乘性效率因子，仅在答案正确时生效，作为比较正确轨迹之间的“加时赛偏好”，鼓励模型用更少的步数找到答案。其形式为 `max(f_min, 1 - λ * max(0, nt - T_d))`。

### 3. 实验设计

*   **数据集**：在三个标准的、基于Freebase的KGQA基准数据集上进行评估：
    *   WebQuestionsSP (WebQSP)
    *   Complex WebQuestions (CWQ)
    *   GrailQA
*   **评估指标**：答案集合上的 Hits@1（精确匹配准确率）。
*   **对比方法**：实验对比分为三大类，非常全面：
    *   **大语言模型基准**：同一8B模型在没有任何KG访问下的IO提示、思维链、自洽性等方法。
    *   **冻 结前沿大语言模型方法**：一系列部署在GPT-4等模型上，通过提示进行图谱导航的系统，如Think-on-Graph、Plan-on-Graph、LMP、iQUEST、Search-on-Graph等。
    *   **后训练大语言模型方法**：包含了语义解析和近期基于SFT/RL的图谱导航模型，如ChatKBQA、KG-Agent、**KG-Hopper (8B)** 和**Explore-on-Graph (8B)**。后二者是与本工作最直接可比的方法。

### 4. 资源与算力

*   **训练硬件**：
    *   **SFT 和 RL 训练**：均使用单个 `8×NVIDIA H200 (141GB)` 节点。
    *   **教师轨迹生成**：通过 vLLM 在单独的 `4×H100` 节点上托管`Qwen3-235B-A22B-Thinking-2507-FP8`模型。
*   **训练时间**：论文附录B提供了详细的超参数，但未明确报告总的训练时长。

### 5. 实验充分性、客观性与公平性

实验设计考虑全面，具有较高的充分性、客观性和公平性。

*   **实验数量**：包含超过15组对比实验，以及至少3组核心消融实验、1组数据规模消融实验、3组路径压缩实验和1组模型泛化实验，分析维度非常丰富。
*   **公平性对比**：这是本文的一大亮点。
    *   **基准模型匹配**：直接与同系列、同参数量（Llama-3.1-8B）的近期工作（`Explore-on-Graph`和`KG-Hopper`）进行对比，这是最公平的较量。
    *   **对比边界清晰**：论文明确指出与`Explore-on-Graph`的输入不同（一个自行检索，一个接收预处理好的子图），因此其结果不能直接线性比较，并对此进行了合理解读，体现了科学的客观性。
    *   **阶段分解清晰**：通过消融实验（Table 3）明确区分了“基础模型”、“纯RL”、“纯SFT”和“SFT+RL”的各自贡献，逻辑严密。
    *   **资源要求透明**：明确指出训练和教师模型所需的具体GPU型号和数量，方便复现和评估成本。

### 6. 论文的主要结论与发现

*   **极致性能**：`Search-on-Graph-R1` (8B) 在三个基准上全面超越了所有“冻结前沿LLM”系统；在CWQ上取得了所有对比系统（包括后训练模型）中的最高分（88.7%）。
*   **训练阶段贡献互补**：SFT提供了最大的单次性能飞跃（冷启动能力），而RL在此基础上带来了进一步但显著的提升，尤其在数据稀缺和SFT基础较弱时作用更大。
*   **路径压缩能力**：RL不仅提高了准确率，还学会了使用比SFT基线更少的`Search`调用次数到达答案（在三个基准上调用次数分别减少15%， 4%， 10%），实现了“多快好省”。
*   **数据效率与泛化性**：数据规模消融实验显示，仅需几千条训练数据就能接近最优性能；方法可成功迁移到另一个模型家族（Qwen3-4B），证明了其泛化潜力。

### 7. 优点

*   **方法论创新**：将“SPARQL-支架蒸馏”与“SFT+GRPO自我优化”相结合，提供了一个优雅的从高成本教师到低成本学生的知识迁移路径。其奖励函数设计将格式控制、准确性和效率融为一体，颇具匠心。
*   **实验严谨全面**：对比基准选择公平，消融实验设计清晰，兼顾了性能上限（vs. 前沿模型）和公平性（vs. 相同骨架的模型），结论具有说服力。
*   **实用性强**：成功将前沿能力压缩进8B模型，在单个`8×H200`节点上即可完成训练，极大地提高了KGQA系统的可部署性，具有很高的实用价值。

### 8. 不足与局限

*   **图谱环境的局限性**：所有实验均基于Freebase。方法对图谱格式（是否支持1跳邻域检索API）和训练数据格式（是否必须有黄金SPARQL查询用于支架）有依赖，向Wikidata等数据集的迁移性未经测试。
*   **理想化的实体链接**：实验遵循了标准设定，即直接提供“黄金主题实体”。这意味着模型跳过了复杂的实体链接（Entity Linking）步骤，其在实际嘈杂环境下的端到端性能可能低于报告值。
*   **代理指标依赖**：部分消融实验（如GrailQA的数据规模消融）显示性能饱和，可能无法完全揭示方法在更大规模或更复杂场景下的潜力。

（完）
