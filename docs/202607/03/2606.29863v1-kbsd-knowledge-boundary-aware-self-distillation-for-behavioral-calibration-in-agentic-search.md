---
title: "KbSD: Knowledge Boundary aware Self-Distillation for Behavioral Calibration in Agentic Search"
title_zh: KbSD：智能体搜索中面向行为校准的知识边界感知自蒸馏
authors: "Tao Feng, Xinke Jiang, Chao Wu"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29863v1"
tags: ["query:fla"]
score: 8.0
evidence: 通过密集token级监督与稀疏奖励的自蒸馏进行智能体搜索知识边界校准
tldr: Agentic搜索中，LLM需动态校准知识边界（何时信任记忆、检索或拒绝），但强化学习存在奖励稀疏问题。KbSD通过提示增强教师注入边界信号，利用信息不对称自蒸馏实现稠密token级监督；并设计象限自适应KL目标（逆KL、前向KL、双向KL）处理异质推理分布。实验表明KbSD在多个基准上提升准确率并降低幻觉，尤其在挑战性象限增益最大，为知识边界校准提供了有效自蒸馏范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29863v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29863v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1780, \"height\": 697, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29863v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29863v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1834, \"height\": 1175, \"label\": \"Table\"}]"
motivation: 现有强化学习方法在知识边界校准中奖励稀疏，缺乏对推理过程的细致指导。
method: KbSD构建提示增强教师，注入知识边界信号生成校准推理，通过信息不对称自蒸馏提供稠密监督，并引入象限自适应KL优化。
result: 在多个基准上，KbSD一致提升准确率与抗幻觉性，在挑战性象限（稀疏奖励最无效处）增益尤为显著。
conclusion: KbSD通过自蒸馏与象限优化缓解了知识边界校准的奖励稀疏问题，为Agentic搜索提供了有效的行为校准范式。
---

## 摘要
智能体搜索赋予大语言模型动态检索能力，但现有强化学习方法受限于知识边界校准中的奖励稀疏性——决定何时信任参数记忆、何时依赖检索证据、何时拒绝回答。二元奖励可以惩罚不良结果，但几乎不为在不同知识状态下做出校准决策所需的推理过程提供指导。为此，我们提出KbSD（知识边界自蒸馏）框架，通过密集的token级监督、结果级稀疏奖励和象限自适应优化来克服这一局限。KbSD构建了一个提示增强的教师模型，其结构与學生模型相同，但接收显式的知识边界信号——包括参数确定性、检索质量和真实答案——以生成校准的推理演示。这种信息不对称的自蒸馏能够在不使用更大的外部模型的情况下实现密集监督。为了进一步考虑知识状态间异构的推理分布，我们引入了一种象限自适应蒸馏目标：针对集中集成使用反向KL散度，针对多样化拒绝使用前向KL散度，针对需要精度和覆盖率的非对称象限使用帕累托最优的双向KL散度。在多个基准上的实验表明，KbSD在任务准确性和幻觉缓解方面均持续优于强基线，最大的提升出现在稀疏奖励最不具信息性的挑战性象限中。

## Abstract
Agentic search equips large language models with dynamic retrieval abilities, but existing reinforcement learning methods remain limited by reward sparsity in knowledge boundary calibration -- deciding when to trust parametric memory, when to rely on retrieved evidence, and when to abstain. Binary rewards can penalize undesirable outcomes, but provide little guidance on the reasoning process required to make calibrated decisions across different knowledge states. To address this, we propose KbSD (Knowledge boundary Self-Distillation), a framework that tackles this limitation through dense token-level supervision, outcome-level sparse rewards, and quadrant-adaptive optimization. KbSD constructs a hint-augmented teacher, architecturally identical to the student, that receives explicit knowledge boundary signals -- including parametric certainty, retrieval quality, and ground-truth answers -- to generate calibrated reasoning demonstrations. This information-asymmetric self-distillation enables dense supervision without requiring a larger external model. To further account for the heterogeneous reasoning distributions across knowledge states, we introduce a quadrant-adaptive distillation objective: reverse KL for concentrated integration, forward KL for diverse refusal, and Pareto-optimal bidirectional KL for asymmetric quadrants requiring both precision and coverage. Experiments on multiple benchmarks show that KbSD consistently improves both task accuracy and hallucination mitigation over strong baselines, with the largest gains appearing in the challenging quadrants where sparse rewards are least informative.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：在大语言模型（LLM）驱动的智能体搜索（Agentic Search）中，**知识边界校准**（Knowledge Boundary Calibration）面临严重的**奖励稀疏**瓶颈。现有的强化学习方法（如 KbPO、Search-R1）只能以二元结果奖励（回答正确／错误）告知模型“该做什么”或“不该做什么”，但无法为中间推理过程提供任何指导，导致模型难以学会何时信任参数化记忆、何时依赖检索、何时整合两者或选择拒绝回答。
- **整体含义**：该文提出，要训练出可靠的智能体，必须在四个知识边界状态（内部已知且检索可靠、仅检索可靠、仅内部可靠、两者均不可靠）下为模型提供**稠密的过程级监督**，而不仅仅是稀疏的结果级奖惩。

### 2. 论文提出的方法论
KbSD（Knowledge-Boundary aware Self-Distillation）框架包含三大核心设计：

- **信息不对称的自蒸馏（Hint-Augmented Self-Distillation）**  
  - 构建一个**提示增强的教师模型**，其网络结构与學生模型完全相同，但在训练时能获得显式的知识边界“提示”（hint），包括参数确定性 `μ`、语义稳定性 `σ`、检索质量 `ρ̂` 以及目标行为象限标签 `c*`。  
  - 教师基于这些提示生成校准的完整推理轨迹；學生模型**看不到提示**，仅在训练时以教师轨迹为密集 token 级监督进行模仿。  
  - 这一设计**无需外部大模型**，实现了同构模型下的信息不对称蒸馏，将稀疏的行为标签转化为稠密的过程监督。

- **知识边界量化与四象限映射**  
  通过无检索采样计算参数确定性 `μ` 与语义稳定性 `σ`，结合检索质量评分 `ρ̂`，将每个查询映射到四种目标行为之一：
  - **集成（Integrated）**：内部已知且检索可靠 → 应整合证据。
  - **外部（External）**：内部未知但检索可靠 → 应依赖检索。
  - **拒绝（Refusal）**：内部未知且检索不可靠 → 应拒答。
  - **内部（Internal）**：内部已知但检索不可靠 → 应信任参数记忆。

- **象限自适应蒸馏目标与联合训练**  
  根据各象限推理分布的特性，匹配不同的 KL 散度形式：
  - **反向 KL**（逆向蒸馏，模式寻求）用于集成象限（推理模式集中）。
  - **前向 KL**（前向蒸馏，质量覆盖）用于拒绝象限（拒答路径多样）。
  - **帕累托最优双向 KL**（自适应加权前向与反向梯度）用于外部和内部象限（需平衡精度与覆盖）。
  
  最终将上述蒸馏损失与 GRPO 强化学习目标联合优化，使模型既受到稠密过程监督，又得到稀疏结果奖励，实现端到端的行为校准。

### 3. 实验设计
- **数据集与场景**：  
  训练集基于 HotpotQA 和 2WikiMultiHopQA 进行合成扩展；评估覆盖 **9 个基准**，分为三类：
  - 多跳问答：HotpotQA, 2WikiMultiHopQA, MuSiQue, Bamboogle
  - 开放域问答：NQ, TriviaQA, PopQA
  - 智能体推理：FRAMES, GAIA

- **对比方法**（三大类共十余个基线）：
  - 标准 RAG：Naive RAG, FS-RAG, FLARE
  - 智能体 RAG：ReAct, IRCoT, Self-RAG, CRAG 等
  - 基于强化学习的智能体：Search-R1, ARPO, AEPO, MEM1, KbPO

- **评估指标**：  
  主要采用 **不可靠率（Unreliability Rate = 1 - (F1 + 正确拒答率)）** 来衡量既未答对又未正确拒答的“不安全”行为比例。辅以 F1 和拒答率分析。

### 4. 资源与算力
- 论文中**未明确说明**所使用的 GPU 型号、数量及具体训练时长。仅提及训练步数为 **300 steps**，每查询生成 **5 个回滚**，batch size 为 128，每个 batch 包含 8 个回滚，学习率 1×10⁻⁶。实际算力需求需结合模型规模（3B、7B）框架（VERL）等估算，但原文未给出具体硬件配置或训练钟时间。

### 5. 实验数量与充分性
- **实验组数**：包含主表（Table 1）在 3B、7B 两个规模下 9 个数据集与十余个基线的全面对比；消融实验（Table 2）对比移除蒸馏损失、固定前向／反向 KL；另有模型规模缩放分析、难度自适应拒答行为分析等。
- **充分性与公平性**：  
  - 对比基线覆盖了当前主流的 RAG、Agentic RAG 以及基于 RL 的方法，具有较好的代表性。
  - 采用统一的不可靠率指标，排除了单纯正确率掩盖过度拒答或幻觉的问题，设计公平。
  - 在多种任务类型上的泛化测试和消融分析较为全面，但训练集仅来自两个多跳数据集，跨领域泛化虽已展现，但未在更多样化的源训练数据上验证。

### 6. 论文的主要结论与发现
- KbSD 在所有基准和模型尺度上均稳定降低了不可靠率，在 3B 和 7B 模型上相较于 KbPO 分别将平均不可靠率从 64.32% 降至 47.60%、从 57.99% 降至 41.18%。
- 最大的改进出现在**稀疏奖励最无效的挑战性象限**（如 Q3 拒绝、Q4 内部依赖），表明稠密过程监督恰好解决了先前方法的痛点。
- 象限自适应的蒸馏目标至关重要，统一使用前向或反向 KL 均会导致性能下降。
- KbSD 展示出良好的域外泛化能力，即使在未训练的 FRAMES、GAIA 上也表现突出。

### 7. 优点
- **问题突破**：明确界定了“过程级奖励稀疏”这一根本瓶颈，并将其与四象限知识状态相联系。
- **信息不对称自蒸馏**：通过同构教师给出的特权提示实现稠密监督，无需额外大模型，方法简洁有效。
- **精馏目标与象限匹配**：根据各象限推理分布的本质差异（集中 vs. 多样）选择不同的 KL 方向，具有较强的动机与设计美感。
- **联合优化**：使 GRPO 产生的稀疏奖励与过程蒸馏提供的稠密梯度互补，既保任务精度又促进行为校准。
- **评价指标合理**：采用“不可靠率”将幻觉与不当拒答统一衡量，更能反映真实可靠性。

### 8. 不足与局限
- **内象限残差问题**：案例研究表明，模型在“内部已知但检索不可靠”的 Q4 象限仍会过度检索，KbSD 尚未完全解决检索效率的校准。
- **训练数据合成依赖**：训练数据由两个多跳数据集合成，边界构建依赖特定阈值和稳定性过滤，不同领域可能需要重新调整阈值。
- **未讨论推理成本与延迟**：方法引入了教师推理和多轮检索，但未分析实际部署中的推理计算开销和响应延迟。
- **模型规模局限**：仅在 Qwen2.5-3B/7B 上实验，未扩展到更大模型或不同架构，且训练仅 300 步，长程训练的稳定性未验证。
- **外部检索的假设**：实验中使用固定的 E5 检索器及 top‑3 段落，现实场景中检索质量波动可能更大，方法的鲁棒性有待进一步检验。
- **奖励与蒸馏权重等超参数**：需依赖手动设置的系数（如 `γ`、`β`、象限阈值），可能对数据集敏感。

（完）
