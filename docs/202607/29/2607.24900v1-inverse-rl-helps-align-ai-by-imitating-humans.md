---
title: Inverse RL Helps Align AI by Imitating Humans
title_zh: 逆强化学习通过模仿人类帮助对齐AI
authors: "Michał Wiliński, Liu Leqi, Chirag Nagpal"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24900v1"
tags: ["query:fla"]
score: 8.0
evidence: 利用从演示中逆向强化学习恢复隐含奖励以对齐语言模型
tldr: 语言模型对齐通常依赖人类偏好标注或验证器奖励，但演示数据隐含的奖励未充分利用。PARED方法通过逆强化学习，用轻量级判别器在响应特征空间区分专家演示与策略样本，恢复显式奖励函数，无需任务特定偏好标注。实验表明，该奖励能在推理重排序和对抗RL中提升基础策略，并在监督微调后进一步优化，还能实现不同受众的上下文对齐。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24900v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 373, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24900v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1410, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24900v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1538, \"height\": 461, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24900v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1665, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1667, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1238, \"height\": 479, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1174, \"height\": 404, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1628, \"height\": 490, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1630, \"height\": 457, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1618, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1520, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1614, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1167, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 863, \"height\": 453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1429, \"height\": 130, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24900v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 862, \"height\": 454, \"label\": \"Table\"}]"
motivation: 当前对齐方法需依赖偏好标注或外部奖励，能否仅通过专家演示恢复可检查、重用的隐式奖励来优化策略。
method: PARED在响应级特征空间训练鉴别器分离演示与策略样本，自动恢复奖励函数，无需额外偏好监督。
result: 恢复奖励在重排序和对抗RL中提高策略性能，并在标准监督微调后带来额外增益，且支持上下文对齐。
conclusion: 从演示中恢复隐式奖励是对齐的有效新途径，PARED减少了对人工标注的依赖，拓展了对齐范式。
---

## 摘要
语言模型对齐旨在使模型行为可靠地反映有用性、安全性和指令遵循等理想属性。当前方法通常使用基于示范的监督微调或基于验证器或人类反馈奖励的强化学习。这些范式忽略了一个重要问题：仅凭示范能否产生可检查、可复用并能通过在线策略优化来对齐AI的隐式奖励？受逆强化学习启发，我们提出了基于示范的投影对齐奖励估计（PARED）。PARED将专家示范背后的隐式奖励恢复为基于少量响应级别特征的显式函数，通过轻量级判别器在特征空间中区分示范与策略自身样本。与标准奖励模型不同，PARED无需特定任务偏好标注：示范提供任务特定监督，并可通过AI反馈作为额外监督维度进行增强。通过推理时重排序和对抗性在线策略强化学习实验，我们展示了恢复的奖励能改进基础策略而无需监督损失，并在标准监督微调后优化时获得进一步增益。此外，我们证明PARED可用于情境对齐，使单一策略根据不同受众的偏好进行定制。

## Abstract
Language model alignment aims to make model behavior reliably reflect desirable properties such as helpfulness, safety, and instruction following. Current approaches typically use supervised fine-tuning on demonstrations or reinforcement learning with rewards derived from verifiers or human feedback. These paradigms leave an important question underexplored: can demonstrations alone yield an implicit reward that can be inspected, reused, and optimized on-policy to align AI? Motivated by inverse reinforcement learning, we introduce Projected Alignment Reward Estimated from Demonstrations (PARED). PARED recovers the implicit reward underlying expert demonstrations as an explicit function over a small set of response-level features, learned by a lightweight discriminator that separates demonstrations from the policy's own samples in this feature space. Unlike a standard reward model, PARED requires no task-specific preference annotations: demonstrations provide the task-specific supervision, which can be augmented with AI feedback as additional dimensions of supervision. Through experiments involving inference-time reranking and adversarial on-policy RL, we show that the recovered reward improves a base policy without a supervised loss and yields further gains when optimized after standard supervised fine-tuning. Additionally, we demonstrate that PARED can be used for contextual alignment, in which a single policy can be tailored to the preferences of different audiences.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：当前语言模型对齐主要依赖两类范式：基于示范的监督微调（SFT，模仿学习）和基于人类偏好或验证器奖励的强化学习（RLHF/RLVR）。这些方法存在一个尚未充分探索的问题——**是否仅凭专家示范就能恢复一个显式、可检查、可复用的隐式奖励，并用于在线策略优化？**
- **整体含义**：论文受逆强化学习（IRL）启发，提出**投影对齐奖励估计（PARED）**。其核心思想是将专家示范视为最佳行为的证据，通过对抗判别的方式，在一个由实践者显式指定的低维**响应级特征空间**内恢复奖励函数。这使得对齐不再依赖昂贵且特定于任务的偏好标注，而是从示范中直接提炼出可审计的对齐目标，并用于推理时选择与在线强化学习优化。

### 2. 论文提出的方法论
- **核心思想**：将语言模型生成视为轨迹，使用一个轻量级判别器 $D_\psi$ 在**投影特征空间** $\phi(\tau)$ 中区分专家示范轨迹 $\mu$ 和当前策略 $\pi$ 生成的轨迹，并将判别器输出的“专家似然得分” $\log D_\psi(\phi(\tau))$ 作为奖励。通过交替更新判别器与策略，实现特征级分布匹配。
- **关键技术细节**：
    - **特征投影 $\phi(\tau)$**：实践者可自定义的低维特征向量。实验中使用：由固定模型 `google/gemma-3-27b-it` 评分的**有用性**和**无害性**（$h/10, s/10$）、基于TF-IDF和LDA的5个**话题分布**（$q_1$–$q_5$），并**显式排除响应长度**（以防成为可控捷径）。
    - **奖励恢复**：训练一个带L2正则化的逻辑回归判别器，其输入为特征向量的**2阶多项式展开**（包含原始坐标、平方项和交互项）。最大化标准二分类对数似然，奖励定义为 $r_\phi(\tau) = \log D_\psi(\phi(\tau))$。
    - **对抗更新流程**：
        1. 生成初始策略样本并拟合初始判别器。
        2. **在线设置**：在策略优化过程中，周期性地用包含历史与最新策略样本的缓冲区重新拟合判别器；**冻结设置**：使用初始判别器并固定。
        3. 使用GRPO优化带KL散度正则项的目标：$\max_\theta \mathbb{E}_{\tau\sim\pi_\theta}[r_\phi(\tau)] - \beta\,\text{KL}(\pi_\theta \| \pi_{\text{ref}})$。参考策略 $\pi_{\text{ref}}$ 可以是基础指令模型或经过SFT的检查点。
- **上下文对齐**：对于不同的受众 $g$（如成人和儿童），使用相同的提示和受众标签作为上下文，但为每个受众训练独立的判别器和奖励 $r_g$。共享一个策略，根据输入中的受众标签使用对应奖励进行优化。

### 3. 实验设计
- **数据集与场景**：
    - **基础提示**：来自 `Anthropic/hh-rlhf` 的有用性和无害性对话。
    - **专家示范生成**：为每个提示-受众对（成人与儿童）预置不同的系统提示，使用 `GPT-5.1` 生成高一致性回答，作为专家示范。
    - **训练/测试集**：**HH-Large-Train**（4000个提示-受众对示范）、**HH-Small-Train**（500个示范，用于低数据条件）、**HH-Large-Test**（1998个示范，仅用于评估）。
- **Benchmark 与对比方法**：
    - **基线**：基础指令微调模型 `Qwen/Qwen2.5-3B-Instruct` 和在其上进行 SFT 的检查点（`SFT-HH-Large`, `SFT-HH-Small`）。
    - **PARED 变体**：从基础模型初始化（*ab-initio*）和从 SFT 模型初始化（*post-hoc*）两种方式；判别器更新方式分为在线（online）和冻结（frozen）。
    - **评估指标**：使用 `google/gemini-2.5-flash` 作为评判器，进行**顺序交换的两两比较**，计算赢率（Win-rate），排除平局分歧。报告 95% 威尔逊置信区间。
- **任务类型**：
    - **推理时重排序（Best-of-N）**：从 16 个 `GPT-OSS-20B` 候选回答中用 PARED 奖励选择最佳，与随机候选比较。
    - **在线策略 RL**：使用 KL 正则化的策略梯度优化策略，评估不同数据量、初始化条件和更新计划的最终赢率。

### 4. 资源与算力
论文中**未明确提及**用于训练的具体算力资源，如 GPU 型号、数量或训练时长。实验采用了多种开源与API模型（GPT-5.1、Gemma 3、Qwen 2.5 3B、Gemini 2.5 Flash），并提到使用了 LoRA 适配器和 vLLM 进行高效生成，但对显存的硬件消耗和总运算时间没有作定量说明。

### 5. 实验数量与充分性
- **实验组数**：进行了多组不同设定下的实验，包括：
    - 1组**推理重排序**实验。
    - **在线RL实验**：2种初始化方案 × 2种判别器更新方案 × 2种数据量（4000 和 500），共 8 个主要条件，加上多个训练周期的检查点扫掠。
    - **消融研究**：冻结 vs. 在线判别器、包含/排除长度特征、仅 LDA 特征 vs. LDA+奖励模型特征、不同数据量（100/500/4000）下的表现。
    - **机制性诊断**：特征距离（MMD²）、判别器 AUC、逐受众赢率分解。
- **充分性评估**：
    - **充分性**：实验设计覆盖面较广，既验证了方法在推理与训练两个环节的效用，又通过多数据量、多初始化和更新计划考察了鲁棒性。消融实验合理地解释了关键设计选择（如排除长度）。
    - **客观公平性**：对比基线包含标准的 SFT，并且使用了独立的评判模型和严格的顺序交换评估协议。作者坦诚指出 500 示范结果是基于相同评估集选择的检查点，属于描述性而非严格统计推断，体现了客观性。整体上，实验较为充分且设计公平。

### 6. 论文的主要结论与发现
1.  **奖励恢复与重排序**：PARED 在低维特征空间上能有效恢复奖励信号，其分数可直接用于推理时重排序，在不改变模型权重的情况下赢得 **63.4%** 的非平局比较。
2.  **策略优化增益**：
    - **大样本（4000 示范）**：*ab-initio* PARED 战胜基础模型的赢率高达**84.6%**；在 SFT 基础上进一步优化的 *post-hoc* PARED 能战胜其 SFT 初始化检查点，赢率达**88.4%**（冻结判别器），证明示范提供了 SFT 无法完全提取的优化信号。
    - **小样本（500 示范）**：*ab-initio* PARED 同样显著优于对应的 SFT 基线（赢率70.7% vs. 58.0%），展示出在数据稀缺时的潜力。
3.  **上下文对齐的有效性**：受众条件化的奖励使单一策略能根据提示同时为成人和儿童生成更合适的行为，且增益对两个受众均有效，不存在此消彼长的权衡。
4.  **特征空间的可审计性**：通过显式排除长度特征，避免了模型通过生成冗长文本骗取高奖励的“捷径”行为，体现了特征设计的实用性和可控性。

### 7. 优点
- **创新性范式**：开创性地将投影特征空间的对抗逆强化学习应用于LLM对齐，仅需示范即可恢复可解释的标量奖励，无需偏好数据。
- **可解释与可审计**：奖励显式地表达为少数可读特征（有用性、无害性、话题）及其交互的线性组合，便于诊断和排除不良行为（如长度作弊）。
- **双阶段通用性**：同一个恢复出的奖励既能用于推理时重排序，也能驱动在线策略优化，具有很好的实用迁移性。
- **超越模仿**：重要发现是 PARED 在 SFT 之上进行在线 RL 仍能带来一致提升，表明对抗性奖励挖掘出了示范中未被直接模仿所利用的结构化信息。
- **设计灵活性**：特征映射 $\phi$ 完全由实践者定义，可灵活融入任何适合特定对齐目标的分布或语义信号。

### 8. 不足与局限
- **示范来源的生态效度**：专家示范由 `GPT-5.1` 按特定提示生成，非真实人类数据。模型可能学到了特定“AI专家”的风格和行为模式，向真人偏好泛化的能力未经验证。
- **评估单一性**：全面依赖 `Gemini 2.5 Flash` 作为评判器，尽管通过交换顺序减少偏差，但仍可能对特定风格或模型族的输出有偏好，导致

导致评估结果与真实人类判断可能存在系统性偏差，且缺乏真正的人类评估作为锚定。

- **示范质量与泛化性**：专家示范由单一的 `GPT-5.1` 模型按刚性提示生成，并非自然人类行为。模型可能过度拟合该特定教师模型的应答风格，在分布外场景或不同文化、语言的受众中泛化能力存疑。
- **特征工程依赖**：特征投影 $\phi$ 完全由实践者事先定义，且依赖于外部奖励模型和主题模型，这引入了额外的设计选择与模型依赖性。当对齐目标难以量化为低维可读特征时，方法的实用性会受限。
- **规模与稳定性限制**：所有实验均基于 3B 参数规模的语言模型，在大规模（如 70B+）或不同架构上的对抗训练稳定性和奖励质量尚未验证。对抗性逆强化学习的训练本身也可能对超参数敏感。
- **未充分利用示范的结构化信息**：当前方法将整个生成路径视为平坦轨迹，未对多步推理、工具调用等结构化对话进行精细建模，可能丢失有价值的时间顺序信号。
- **计算与评估的间接性**：虽然论文未报告算力消耗，但在线对抗更新需要交替训练判别器和策略，引入了额外的计算负担；且完全依赖大模型评判器间接评估，缺少直接的人类偏好一致性研究。

（完）
