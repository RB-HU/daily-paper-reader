---
title: "CHILL-Harness: Counterfactual Harness Learning for Efficient Reasoning in Long-Horizon Agents"
title_zh: CHILL-Harness：面向长时程智能体高效推理的反事实操控学习
authors: "Jiarun Fu, Lizhong Ding, Sida Chen, Honglei Xin, Chunhui Zhang, Pengqi Li, Qiuning Wei, Ye Yuan, Guoren Wang"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25825v1"
tags: ["query:fla"]
score: 9.0
evidence: 使用反事实推理学习智能体 harness 的自适应编排策略
tldr: 长时域智能体依赖的编排系统多采用固定策略，导致计算冗余且可能降低任务成功率。为此提出CHILL-Harness框架，通过因果干预学习实现工作流自适应调整。实验表明该方法在多种长时域任务中保持或提升成功率，同时大幅减少推理成本。该研究为构建高效可靠的长时域智能体提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25825v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1832, \"height\": 971, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25825v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 708, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25825v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1689, \"height\": 651, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25825v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1479, \"height\": 537, \"label\": \"Table\"}]"
motivation: 当前智能体编排系统依赖手工或固定策略，计算开销大且影响任务成功率。
method: 基于因果学习，通过优势引导的干预效应估计与自适应工作流分配实现编排优化。
result: 在信息寻求、软件工程和终端交互等任务上，保持成功率并显著降低令牌消耗和执行时间。
conclusion: CHILL-Harness通过因果自适应编排有效平衡了长时域智能体的效率与可靠性。
---

## 摘要
智能体操控基础设施已成为现代大语言模型智能体的运营基础，通过协调上下文、工具、验证与执行控制，将潜在的模型能力转化为可靠的长时程行为。然而，可靠的长时程行为要求操控控制能适应任务需求、执行环境与动态的执行状态，而当前的操控系统主要依赖人工设计或全局固定的策略；这种不匹配表现为不必要的计算开销，并在不利情形下导致任务成功率下降。为解决这一局限，我们将操控系统中实现自适应编排的任务形式化为一个因果学习问题，并提出面向长时程智能体的反事实操控干预学习（CHILL-Harness）。CHILL-Harness在编排层面进行干预，以实现优势引导的工作流适配，从而在保持任务性能的同时提升推理与执行效率。具体而言，我们开发了因果干预效应学习作为CHILL-Harness的效应估计组件，从置信度加权执行证据中估计干预相对的工作流优势，并识别有利的工作流调整。我们进一步引入了优势实现因果编排作为其实现组件，以自适应地分配反事实推理，并仅实现由足够期望优势支持的工作流调整。最后，我们将成功保持目标与优势裕度授权约束融入CHILL-Harness，以促进可靠的适应性调整。在涵盖信息搜索、软件工程和终端交互的异构长时程任务上进行的广泛实验表明，CHILL-Harness在显著降低令牌消耗和执行时间的同时，持续保持或提升了任务成功率。

## Abstract
Agent harnesses have become the operational infrastructure of modern large language model agents, coordinating context, tools, verification, and execution control to translate latent model capability into reliable long-horizon behavior. However, reliable long-horizon behavior requires harness control to adapt to task demands, execution environments, and evolving execution states, whereas current harnesses predominantly rely on hand-crafted or globally fixed policies; this mismatch manifests as unnecessary computational overhead and, in adverse cases, reduced task success. To address this limitation, we formulate the task of enabling adaptive orchestration in harness systems as a causal learning problem and propose Counterfactual Harness Intervention Learning for Long-Horizon Agents (CHILL-Harness). CHILL-Harness intervenes at the orchestration layer to enable advantage-guided workflow adaptation, thereby improving reasoning and execution efficiency while preserving task performance. Specifically, we develop causal intervention effect learning as the effect-estimation component of CHILL-Harness to estimate intervention-relative workflow advantage from confidence-weighted execution evidence and identify advantageous workflow adaptations. We further introduce advantage-realizing causal orchestration as its realization component to adaptively allocate counterfactual reasoning and realize only workflow adjustments supported by sufficient expected advantage. Finally, we incorporate a success-preserving objective and advantage-margin authorization constraints into CHILL-Harness to promote reliable adaptation. Extensive experiments on heterogeneous long-horizon tasks spanning information seeking, software engineering, and terminal interaction show that CHILL-Harness consistently preserves or improves task success while substantially reducing token consumption and execution time.

---

## 论文详细总结（自动生成）

好的，根据提供的论文内容，以下是对《CHILL-Harness: Counterfactual Harness Learning for Efficient Reasoning in Long-Horizon Agents》的结构化中文总结。

### 1. 论文的核心问题与整体含义
- **研究背景与动机**：大语言模型智能体在解决长时程任务时，依赖于被称为“操控基础设施”的系统来协调上下文、工具和执行。然而，现有操控系统（如OpenHands， Terminus-KIRA等）多采用人工设计或全局固定的工作流策略，无法根据动态的任务需求、环境状态和执行进展进行自适应调整。
- **核心问题**：这种“固定策略”与“动态需求”之间的不匹配，导致在长时程执行中产生大量的冗余推理和计算开销，甚至在某些情况下会降低任务的成功率。因此，核心问题是如何让操控系统具备自适应的编排能力。
- **整体含义**：论文旨在提供一个统一的框架，使操控系统能学习“是否”以及“如何”对当前工作流进行干预，从而在保证任务成功率（甚至提升）的同时，大幅提升推理和执行效率。

### 2. 论文提出的方法论
CHILL-Harness 框架的核心思想是将自适应编排形式化为一个**因果学习问题**，通过在编排层面进行“反事实干预”，评估并采纳税有显著优势的工作流调整。该框架分解为两大组件和一套约束：

- **核心组件一：因果干预效应学习**
    - **目标**：解决“干预评估问题”，即判断一个潜在的工作流调整是否优于当前的事实工作流。
    - **关键技术细节**：
        1.  **统一干预空间**：将异构的编排行为（如额外推理`Deliberate`、修正证据`Revise`、终止冗余`Stabilize`等）统一为语义化的干预意图 `I_wf_t`，并将这些意图映射到具体的可执行工作流 `G_t(ι)`。
        2.  **离线效应估计**：通过离线“检查点回放”方式，在同一执行上下文下执行事实工作流和候选工作流，计算两者的效用差值 `Γ_t(ω)` 作为反事实干预效应的监督信号，训练一个工作流效应估计器 `Γ̂_φ`。
        3.  **在线摊销预测**：将学习到的干预偏好蒸馏到一个在线预测模型，使其能根据当前上下文直接预测最优干预 `ι̂_t`，避免了在运行时进行昂贵的搜索。
        4.  **因子化决策**：将干预的实现分解为三个决策头：**反事实审议效应**（决定需要多少额外推理）、**反事实修正效应**（决定是否用候选工作流替换事实工作流）和**完成归因效应**（判断任务是否应完成）。

- **核心组件二：优势实现因果编排**
    - **目标**：解决“干预实现问题”，即在给定估计效应的基础上，安全、有效地执行工作流调整。
    - **算法流程**：采用“先路由，后生成”的策略，其流程如下：
        1.  **路由选择**：`CDE`预测一个审议路线，系统根据资源预算和可行性，选择一个具体的、成本受控的路线 `z_t`（`Skip`、`Light`或`Full`），从而限制候选工作流的生成范围。
        2.  **候选生成与筛选**：根据选定的路线和干预意图，生成候选工作流集合，并进行安全性和任务兼容性过滤，得到可用的候选集 `Ω_t`。
        3.  **因果-操作联合估值**：对每个可用候选，将其**操作效用**与**估算的工作流效应**结合，计算综合效用 `Û_ARCO`。
        4.  **保守授权**：选出综合效用最高的候选工作流 `ω_t^+`，但只有当`CRE`预测其“足以支持切换”，且其综合优势超过一个预设的**优势裕度** `δ` 时，才授权执行该候选工作流；否则保留原事实工作流。任务终止同样需要`CAE`的预测与任务验证器的双重确认。

- **训练约束**：引入一个**成功保持目标**和**优势裕度授权约束**。该目标在最小化轨迹成本的同时，惩罚导致任务失败或将参考成功案例转为失败的调整，确保效率提升不以牺牲可靠性为代价。

### 3. 实验设计
- **基准数据集**：实验在三个具有代表性的长时程任务基准上进行：
    - **GAIA**：用于评估信息检索和工具辅助推理能力。
    - **SWE-bench Verified**：用于评估代码仓库级别的软件修复能力。
    - **Terminal-Bench 2.0**：用于评估复杂的命令行终端交互能力。
- **对比方法**：将CHILL-Harness与每个基准上表现突出的公共系统进行比较。例如，在GAIA上与`Terminus-KIRA`、`AWorld`、`OWL Workforce`对比；在SWE-bench Verified上与`OpenHands`和`CodeSweep–SWE-agent`对比；在Terminal-Bench 2.0上与`Meta-Harness`和`LemonHarness`对比。同时，还通过与未使用CHILL-Harness的同一模型、工具的`参考运行`进行配对比较，以评估成功保持性。
- **消融实验**：设计了`Always-Full`变体（始终执行`Full`级别的审议）来对比，以验证自适应审议路由机制的贡献。

### 4. 资源与算力
- 论文中**未明确提及**具体的计算资源消耗，例如使用了何种GPU、数量、以及离线训练的总时长。
- 关键的算力相关说明包括：
    - 离线训练阶段存在独立的成本，该成本**不包含**在测试时的令牌或运行时间测量内。
    - 测试时的令牌总数计算**包括了**为反事实审议和候选价值评估而产生的所有额外模型调用（如规划器调用）。
    - 消融实验的表2中报告了“额外规划器令牌消耗”和“累积规划器延迟”作为效率指标，但未涉及底层硬件信息。

### 5. 实验数量与充分性
- **实验组数**：实验覆盖了**3大异构领域基准**的**8个公开基准系统**（未计匹配参考）的比较，以及**1个消融实验**，总量较大，覆盖了信息检索、软件工程和系统交互等不同任务类型。
- **充分性与公平性**：实验设计总体上是**充分且客观**的。
    - **公平性**：通过匹配参考运行（同模型、工具、环境）排除了底层模型能力带来的干扰，专注于评估编排框架本身的增益。与公开系统的对比进一步展示了其外部竞争力。
    - **有效性**：成功率和令牌效率是衡量长时程智能体的核心通用指标。消融实验明确指出了所提方法中关键模块（自适应审议路由）的价值。
    - **潜在偏差**：虽然比较设计公平，但`CIEL`模块的离线训练数据构建（检查点回放）可能会引入选择偏差，训练数据的收集策略和质量会直接影响模型性能的上限，文中虽有说明，但其具体影响在实验中未被量化分析。

### 6. 论文的主要结论与发现
- CHILL-Harness 能够有效解决提出的因果编排问题，它在三个异构的长时程任务基准上，**一致地保持甚至提升了任务成功率**。
- 与现有最低成本的公共基准系统相比，CHILL-Harness 显著降低了推理成本，表现为**令牌消耗减少13.1%至28.4%，执行时间缩短2.6%至46.6%**，实现了最高的“每百万令牌解决任务数”。
- **消融实验证实了组件设计的必要性**：强制进行完全审议（`Always-Full`）会导致过度干预、增加规划器开销，并降低部分任务的成功率，证明了自适应路由机制的不可或缺性。

### 7. 优点
- **创新性的因果视角**：首次将因果推断中的反事实框架系统性地引入到智能体编排层决策上，将抽象的自适应问题转化为可学习的“效应评估”问题。
- **清晰的问题分解与设计**：将编排问题分解为效应估计和实现两大组件，并进一步用因子化决策头将“做什么”、“做多少”和“要不要做”解耦，结构清晰，可解释性强。
- **“路由在生成之前”的设计原则**：通过在生成候选工作流之前就决定推理深度，从根源上避免了昂贵的无效计算，这是效率提升的关键，并被消融实验所证明。
- **坚实的成功保持机制**：通过成功保持损失函数、优势裕度和双重终止验证等保守授权机制，确保了效率提升不以牺牲可靠性为代价，实验结果很好地支撑了这一点。
- **稳健的泛化能力验证**：在三种性质截然不同的长时程任务上（信息检索、代码修复、终端交互）均展示出一致的性能优势，证明了方法的通用性。

### 8. 不足与局限
- **缺乏计算成本说明**：论文完全没有提供离线训练所需的算力（GPU型号/数量）和时间，这是评估方法实用性的一个重要缺失。
- **离线训练的依赖性**：`CIEL`模块依赖昂贵的“世界模型”（通过检查点回放探索实际执行结果）进行训练，其训练数据的获取方式、质量和覆盖范围是潜在的性能瓶颈，方法性能的上限受限于此。
- **权重与阈值的敏感性**：框架中引入了多个超参数（如损失函数中的`λ`，操作效用中的`α`，授权裕度`δ`和效应权重`η_Γ`），论文未讨论这些参数在不同任务上的敏感性及调优策略，这可能会影响其在全新领域应用的便捷性。
- **实验的精细化程度**：消融实验仅针对审议路由（`CDE`）进行了整体分析，缺少对`CIEL`中其他决策头（如`CRE`、`CAE`）或关键超参数的独立消融研究，难以精确评估每个模块的价值。
- **对验证器的依赖**：任务完成机制（`CAE`结合任务验证器`V_t`）严重依赖于特定基准的环境信号，其性能可能因不同任务或环境中验证信号的强弱而产生波动。

（完）
