---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连接点：通过强化学习训练具备跨域泛化能力的长周期智能体的大型语言模型
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:fla"]
score: 9.0
evidence: 提出基于强化学习的自主智能体后训练方法，包含长轨迹回放，直接匹配自主智能体后训练技术。
tldr: 为解决长期AI代理需持续积累环境经验以提升多任务表现的问题，本文提出CoD框架，采用端到端强化学习与GRPO算法，训练LLM在交替的任务执行与上下文更新中习得元能力。实验表明，该方法能有效赋予模型跨任务泛化潜能，尤其在分布外领域和Ralph-loop设置中表现突出，为构建自适应AI代理提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 长期运行的AI代理需在多任务序列中持续学习并自我改进，但现有训练方法难以赋予其跨任务泛化的元能力。
method: 提出CoD框架，采用端到端强化学习与GRPO算法，在交替的任务解决与上下文更新中训练LLM，并设计特定环境以激发元能力。
result: 实验验证了端到端强化学习在CoD设置中的有效性，模型展现出分布外泛化潜能，包括跨不同领域及Ralph-loop设置。
conclusion: CoD框架连接了多个研究方向，通过开源实现为构建自进化LLM代理提供了新可能，推动了AI代理的长生命周期泛化能力。
---

## 摘要
本文提出了一个通用框架，用于训练大型语言模型（LLMs）具备“连接点”（CoD）的元能力，这是长周期智能体所需的能力：当基于LLM的AI智能体部署在环境中时，它解决一系列长期任务，同时持续探索环境，从自身经验中学习，并迭代地自我更新其对环境的上下文，从而在更新后的上下文条件下，逐步提升未来任务的表现。CoD框架的主要组成部分包括：（1）算法设计和基础设施，用于端到端强化学习（RL），具有长rollout序列，交织解决任务和更新上下文的片段；（2）任务和环境，用于在训练过程中激励和引出LLM中目标元能力，以及在评估过程中忠实地衡量进展。我们展示了CoD框架的概念验证实现，包括带有细粒度信用分配的GRPO风格RL算法，以及针对目标元能力（而非领域特定的LLM能力或标准的逐任务RL）定制的任务和环境。实证结果验证了在CoD设置下端到端RL训练的有效性，并证明了所引出元能力在分布外泛化的潜力——在训练领域内、跨不同领域、以及从CoD到Ralph-loop设置——。我们对CoD的研究连接了多个先前工作线，并为推进LLM和AI智能体开辟了新机遇。为了促进进一步的研究和应用，我们在https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod上发布了实现。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

好的，以下是基于论文《Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning》的详细中文总结。

### 1. 论文的核心问题与整体含义

本论文旨在解决**长生命周期智能体**的核心能力缺口。当前，基于大型语言模型（LLM）的AI智能体在部署于真实环境时，难以像人类一样通过持续探索、从自身经验中学习和迭代更新对环境认知的方式，来逐步提升后续任务的表现。这种能力被作者定义为“连接点”（Connect the Dots, CoD）元能力。现有的标准强化学习（RL）训练范式多为“逐任务”进行，即要求模型每次都从零开始独立解决单个任务，这与长期部署中任务间需知识传递、上下文累积的需求相悖。

因此，本文的核心动机是**设计一个专门的框架，通过端到端强化学习来显式地训练和激发LLM的这种CoD元能力**，使智能体能够在测试期持续自适应、自我改进，以应对多样化的环境。

### 2. 论文提出的方法论

论文提出了 **CoD框架**，其包含部署（CoD-Deploy）和训练（CoD-Train）两个对称的层面。

*   **核心思想**：
    *   **CoD-Deploy**：抽象了智能体在环境中的长期部署过程，即交替进行解决新任务（`solve-task`）和自我更新关于环境的上下文（`update-context`）的片段。
    *   **CoD-Train**：其RL训练的回放模式与CoD-Deploy完全一致，训练智能体在交替的任务与更新片段中最大化累积奖励，从而将任务间依赖关系纳入学习目标。

*   **关键技术细节与算法流程**：
    *   **信用分配**：这是其RL算法设计的一大挑战。由于每个训练回放轨迹包含多个解决任务和更新上下文的片段，需合理分配奖励。论文遵循**动态规划原理**，定义每个片段的回报为**当前片段的即时奖励 + 未来解决任务片段的平均奖励**。更新上下文片段虽无直接任务奖励，但其价值通过提升未来任务表现来体现。
    *   **优势计算**：采用**GRPO风格**的算法，但做了关键适配。将不同回放轨迹中处于相同位置（如均为第一个`update-context`片段）的片段归为一组，计算该组的平均回报作为**基线**。单个片段的优势即为其回报减去同位置基线。
    *   **训练稳定性增强**：针对观察到的训练不稳定问题，引入了一种**自适应重加权启发式方法**。当批次数据的平均优势为负时，对梯度更新公式中的优势项施加一个以温度为参数的指数重加权（`exp(Ai/T)`），以平衡正负梯度，稳定训练过程。最终采用结合**REC-OneSide-NoIS**变体与上述重加权机制的梯度计算公式。
    *   **上下文管理**：在概念验证阶段，智能体的“上下文”被简化为一段文本“提示”（`hint`），附加在系统提示中。

### 3. 实验设计

*   **使用场景/数据集**：
    论文设计了三个专用合成或仿真环境来训练和评估CoD元能力，而非使用通用NLP基准。
    1.  **FrozenLake-Obscure**：经典FrozenLake的变体，关键在于行动到移动方向的映射在每次新环境初始化时随机排列且未知。这强制智能体必须通过交互发现映射关系，并将其作为上下文传递给后续任务。
    2.  **Alchemy-Random**：经典Alchemy基准的变体，每个环境的元素合成配方是随机生成且未知的。智能体需通过实验发现有效配方并记录失败组合，以更高效地合成目标元素。
    3.  **TerminalSimulator**：模拟Linux/MacOS/Windows终端操作的任务集合，用于测试跨领域泛化。

*   **评估基准与对比方法**：
    *   **主要验证**：对比CoD-Train训练**前后**模型在任务序列中不同位置（`pos 0, 1, 2, 3...`）的奖励变化，以体现上下文累积带来的增益。
    *   **泛化性评估**：
        *   **领域内分布外（In-domain OOD）**：在相同训练领域但**更困难**（如更大地图、更多元素）和**更长任务序列**（长度从4增至8）的环境下评估。
        *   **跨领域（Cross-domain）**：在训练时未见的领域（如仅在FrozenLake训练，却在Alchemy或TerminalSimulator评估）进行测试。
        *   **Ralph-loop设置**：即同一任务解决多次的循环设置，检验CoD能力能否泛化到更简单的自我改进场景。
    *   在RL算法层面，对比了**Vanilla GRPO**、**REC-OneSide-NoIS**以及**本文最终的算法**在训练稳定性上的表现。

### 4. 资源与算力

*   论文未明确提及训练所使用的具体GPU型号、数量及训练总时长。

### 5. 实验数量与充分性

*   **实验数量**：论文设计了**两组主要训练实验**（Setting A: 仅在FrozenLake-Obscure上训练; Setting B: 在FrozenLake-Obscure和Alchemy-Random混合域上训练），并对每个设置的模型进行了**四项评估**（训练奖励曲线、领域内OOD、跨领域OOD、Ralph-loop）。此外，还包括一个算法对比的**消融实验**。
*   **实验充分性**：实验设计思路清晰，针对CoD框架的**有效性**和**泛化性**两大问题设计了层次分明的验证流程。从一个最小测试集（FrozenLake-Obscure）开始，逐步扩展到混合域训练和更复杂的OOD场景，具有较好的逻辑性。然而，实验环境均为合成或仿真环境，**缺乏与真实世界应用场景（如论文提到的个人助理、代码仓库维护）的联系**。总共两个训练设置和有限的消融研究，体量上可视为**概念验证**，充分性有待后续工作加强。
*   **客观性与公平性**：作者对结论持审慎态度，如对TerminalSimulator上的跨域性能提升给出了合理解释和保留意见，指出其可能源自任务间关联性不强而非CoD。对比的基线是未经CoD训练的初始模型，较为公平。算法消融也直接对比了不同变体，展示了改良的有效性。

### 6. 论文的主要结论与发现

1.  **CoD-Train的有效性**：通过端到端RL，模型在任务序列中的表现得到显著提升。例如，在FrozenLake-Obscure环境中，任务序列中第4个任务的成功率可从28%提升至76%，远超从零开始解决任务的表现（从18%到45%）。
2.  **泛化潜力**：训练出的CoD元能力展现了良好的泛化性，能在同领域内更难的任务、不同领域的新环境，甚至Ralph-loop式的反复尝试场景中带来性能增益。
3.  **算法组件的重要性**：精细化的片段级信用分配和训练稳定性策略（如自适应重加权）对于CoD训练的成功至关重要，标准GRPO等方法会面临训练不稳定问题。

### 7. 优点

*   **问题定义新颖且重要**：明确区分了“领域专有能力”和“跨任务学习、自我进化的元能力”，并提出了系统性的训练框架，填补了相关空白。
*   **方法设计系统**：CoD框架从部署（CoD-Deploy）和训练（CoD-Train）两端出发，逻辑对称，且与动态规划和分级强化学习等经典理论紧密联系。
*   **算法适配性强**：对GRPO算法进行了精细化的信用分配改造，使其适应长序列、多片段交织的CoD训练模式。
*   **环境设计针对性强**：FrozenLake-Obscure等环境的设计精巧，有效地剥离了先验知识的干扰，创设了必须依赖上下文累积才能突破信息上限的场景，是验证CoD能力的最小必要条件。

### 8. 不足与局限

*   **实验环境的规模与真实性有限**：所有实验均在小型、合成的游戏或仿真环境中进行，尚未验证其在复杂、开放且需要管理海量上下文（如代码库、用户长期画像）的真实世界场景中的可行性。
*   **上下文管理机制过于简化**：当前实现中，跨任务传递的“上下文”仅为一段不断重写的“提示”（hint）。这远未达到论文展望中如持久化记忆库、可复用技能集等更强大的上下文管理形式。
*   **RL算法仍有启发式成分**：为稳定训练而引入的自适应重加权方法带有启发式色彩，作者也承认未来需要更原则化、理论基础更强的RL算法。
*   **泛化性验证的有限性**：跨领域泛化仅在有限领域间进行，且部分结果（如TerminalSimulator上的提升）的归因尚不明确，结论的强度和普适性仍需更多证据支持。
*   **算力信息缺失**：论文未报告训练所需的算力资源，这使得他人难以评估和复现其计算成本。

（完）
