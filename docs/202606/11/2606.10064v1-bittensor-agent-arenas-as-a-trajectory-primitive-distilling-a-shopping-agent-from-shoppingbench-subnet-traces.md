---
title: "Bittensor Agent Arenas as a Trajectory Primitive: Distilling a Shopping Agent from ShoppingBench Subnet Traces"
title_zh: Bittensor智能体竞技场作为轨迹原语：从ShoppingBench子网轨迹中蒸馏购物智能体
authors: "Shardul Bansal, Seth Schilbe, Jarrod Barnes"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10064v1"
tags: ["query:fla"]
score: 9.0
evidence: 利用智能体竞技场的多轮轨迹进行智能体后训练
tldr: "小型智能体后训练受限于多轮轨迹基座质量，合成数据带偏差且长尾坍塌，生产日志无评判且含捷径。本文利用Bittensor ORO子网15激励对齐竞技场生成轨迹，通过结构过滤保留agentic交互，对Qwen3-4B进行SFT+GRPO后训练。在购物任务上ASR从18.0%升至42.7%，接近合成数据SFT基线，Dr. GRPO奖励有利于缩小pass@8与pass@1差距。该工作证明竞技场轨迹可作为低成本蒸馏基座，并公开过滤器和语料以促进研究。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10064v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1792, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10064v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10064v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10064v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1997, \"height\": 826, \"label\": \"Table\"}]"
motivation: 小模型智能体后训练缺乏带评判的多轮轨迹，合成数据有模型偏见和长尾分布坍塌，未过滤日志混杂捷径行为。
method: 基于Bittensor SN15竞技场生成轨迹，用结构质量过滤器区分agentic与子任务轨迹，对Qwen3-4B执行SFT-then-GRPO后训练。
result: "在防泄露评估集上，模型ASR从18.0%提升至42.7%，逼近合成数据SFT基线43.6%，Dr. GRPO奖励提升过程质量并缩小pass@k差距。"
conclusion: 激励对齐竞技场可批量制造高质量训练轨迹，结合过滤与后训练，能高效蒸馏小模型购物智能体，且子任务数据是进一步改进的关键杠杆。
---

## 摘要
小模型智能体后训练更多受限于其所消耗的轨迹基底，而非算法。当前主流方法（RLVR、组相对RL、拒绝采样重SFT）都需要携带逐轨迹监督的多轮轨迹，而现有两种来源存在不足：前沿模型合成的数据继承了合成器的偏差并压缩了长尾分布，未经筛选的生产日志缺乏评判且被捷径行为污染。我们认为可设计一个激励对齐的智能体竞技场来制造此类轨迹，并在ORO子网15（SN15）上加以演示，该子网是ShoppingBench智能体商务基准的Bittensor部署。SN15的竞赛机制、LLM推理评判器以及轮换的防泄漏问题套件，产出了一个具有三种性质的语料库：激励对齐的多样性、逐轨迹评判和防记忆的留出评估。我们引入了一个结构质量过滤器，通过保留智能体轨迹（模型自行发出工具调用）并拒绝子任务轨迹（模型仅在确定的搜索循环上进行分类或叙述），将原始数据洪流转化为可训练的语料库，然后使用与已发布的ShoppingBench SFT后接GRPO流程相匹配的方法对Qwen3-4B进行后训练。在防泄漏的留出划分上以严格生产标准评分，模型将已发布的Qwen3-4B基线的18.0% ASR提升至42.7%，仅使用合成数据SFT的基线（43.6%）在单问题噪声范围内相当，而训练仅消耗了单天子网输出的一小部分。监督学习栈留下了较大的pass@8与pass@1差距（53.3%对34.8%）；一个基于教师信号的逐步Dr. GRPO奖励将这一余地转化为过程改进，我们发现子任务洪流是缩小与48.7% SFT+GRPO标杆差距的主要杠杆。我们公开了过滤器、语料划分以及竞技场机制。

## Abstract
Small-model agentic post-training is bottlenecked less by the algorithm than by the trajectory substrate it consumes. Leading recipes (RLVR, group-relative RL, rejection-sampled re-SFT) all need multi-turn traces carrying per-trajectory supervision, and the two existing sources fall short: frontier-synthesised data inherits the synthesizer's biases and collapses the long tail, while unfiltered production logs are unjudged and contaminated by shortcut behaviour. We argue that an incentive-aligned agent arena can be engineered to manufacture such trajectories, and demonstrate this on ORO Subnet 15 (SN15), a Bittensor deployment of the ShoppingBench agentic-commerce benchmark. SN15's race mechanism, LLM reasoning judge, and rotating leak-cluster-guarded problem suite yield a corpus with three properties: incentive-aligned diversity, per-trajectory judging, and anti-memorised held-out evaluation. We introduce a structural-quality filter that converts the raw firehose into a trainable corpus by keeping agentic trajectories (the model itself emits the tool calls) and rejecting sub-task trajectories (the model only classifies or narrates over a deterministic search loop), then post-train Qwen3-4B with a recipe matched to the published ShoppingBench SFT-then-GRPO pipeline. On a leak-cluster-guarded held-out partition scored production-strict, the model lifts from the published Qwen3-4B base of 18.0% ASR to 42.7%, within single-problem noise of the synthetic-data SFT-only baseline (43.6%), while training on a fraction of a single day of subnet output. The supervised stack leaves a large pass@8 to pass@1 gap (53.3% vs 34.8%); a per-step teacher-grounded Dr. GRPO reward converts that headroom into process improvement, and we identify the sub-task firehose as the primary lever for closing the gap to the 48.7% SFT+GRPO bar. We release the filter, the corpus splits, and the arena mechanics.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：小型语言模型在工具调用、网页浏览等智能体任务上的后训练，其性能提升的瓶颈不在于算法选择，而在于训练所用的多轮交互轨迹基材（trajectory substrate）的质量。
*   **现有轨迹基材的局限**：
    *   **完全由前沿模型合成（如ShoppingBench、ToolLLM）**：合成器的固有偏见会传递到数据中，且纯自蒸馏过程会导致数据分布的长尾部分坍塌，缺乏多样性。
    *   **未经过滤的生产日志**：虽然包含真实多样性，但缺乏对每条轨迹的评判，数据极度不平衡，且常被模型的“捷径行为”（靠不合理的推理得出正确答案）所污染。
*   **整体含义**：本文提出并验证，可以将一个激励对齐的智能体竞技场（一个运行着持续比赛的Bittensor子网）刻意设计成一种生成高质量多轮轨迹的新型“数据工厂”。通过这种方式产出的轨迹，同时具备激励对齐的多样性、逐轨迹评判和防记忆化的留出评估三大特性，从而为小模型智能体的后训练提供了一个廉价且有效的轨迹基材。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法或流程

*   **核心思想**：利用一个经济激励的、持续的智能体竞赛（Bittensor子网），将大量独立参与者（矿工）为获取奖励而不断优化的交互过程，转化为带评判的训练轨迹，再通过一个结构质量过滤器筛选出高质量的“智能体式”轨迹，用于蒸馏一个小型购物智能体。
*   **关键技术细节与流程**：
    *   **竞技场机制（SN15）**：
        *   **激励对齐的多样性**：矿工提交智能体参与排位赛和每日竞赛，排名靠前的获得代币奖励。源码定期开源，促使参与者不断发现新策略。
        *   **逐轨迹评判**：每条轨迹由一个基于LLM的**推理质量评判器**进行评分。该评判器会引用轨迹中的具体片段作为证据，生成一个推理系数，与基于规则的结果得分相乘，构成逐轨迹的过程监督信号。
        *   **防记忆化留出评估**：使用“泄露簇”（leak cluster）保护机制，将语义等价但表达不同的问题聚类，确保留出集（evaluation pool）和训练集（training pool）之间在问题层面完全隔离，而非仅停留在轨迹层面。
    *   **数据管道与结构质量过滤器**：
        *   **格式规范化**：使用 `Trac` 工具将原始轨迹统一转换为符合OpenAI工具调用模式的格式。
        *   **三级过滤**：
            1.  **推理系数门**：丢弃推理质量系数低于阈值的轨迹。
            2.  **格式有效性门**：硬性拒绝违反5条结构不变量的轨迹（如工具调用-响应不匹配、无推荐即终止等）。
            3.  **去重与排名**：在每个问题内部，根据思考深度、搜索重构次数、验证步骤等结构质量信号对轨迹进行排名，择优选取。
        *   **轴A/轴B分离**：保留“**轴A（智能体式）**”轨迹（LLM本身发出工具调用），拒绝“**轴B（子任务式）**”轨迹（Python代码发出工具调用，LLM仅用于分类或叙述）。这是保证模型学到智能体策略的关键。
    *   **后训练流程**：
        *   基于 **Qwen3-4B** 模型，采用与已发布的ShoppingBench论文相近的SFT-then-GRPO框架。流程分为五个阶段：
            1.  **SFT**：在过滤后的语料上进行监督微调。
            2.  **拒绝采样重SFT (Rejection-Sampled re-SFT)**：用当前模型采样多条轨迹，只保留满分轨迹并入SFT语料再微调。
            3.  **基于教师模型的持续SFT (Continued Teacher-SFT)**：将昂贵的在线策略蒸馏（OPD）替换为在Sonnet 4.6（教师）的满分轨迹上进行持续微调。
            4.  **KTO偏好优化**：使用无需配对数据的KTO算法进行偏好对齐，实验显示其仅在桶内重新分配概率，未提升整体能力。
            5.  **Dr. GRPO逐轮强化学习**：在最终的SFT模型上，使用基于教师信号的、逐步骤的工具匹配奖励进行Dr. GRPO强化学习，旨在将pass@k的潜在能力转化为实际的pass@1提升。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

*   **数据集/场景**：所有训练和评估均基于**ShoppingBench**智能体商务基准。这是一个索引式的购物任务，要求智能体在给定自然语言购物意图后，通过多次工具调用（搜索、查看商品/店铺、核对属性等）从静态产品目录中找到满足价格、服务、SKU和属性四大类规则约束的商品。
*   **基准（Benchmark）与评估方式**：
    *   **评估集**：一个包含75个问题的、经过泄露簇保护的留出集（held-out partition）。
    *   **核心指标**：平均成功率（ASR），即最终推荐的商品同时满足所有规则约束的问题比例。
    *   **评分标准**：采用与生产环境一致的严格评分（production-strict）。
*   **对比方法**：
    *   **已发表基线**：ShoppingBench论文中的 Qwen3-4B 基础模型（18.0% ASR）、Qwen3-4B + SFT（合成数据，43.6%）、以及其 SFT+GRPO 的标杆（48.7%）。
    *   **前沿闭源模型**：GPT-5.5（38.7%）和作为教师的Claude Sonnet 4.6（64.0%）。
    *   **SN15竞赛顶级模型**：一个由多种LLM组成的集成系统（77.3%）。
    *   **本文方法的不同变体**：详细对比了自身后训练流程中各个阶段的性能（如仅SFT、rejection v2、opd sonnet、最终SFT栈、KTO及多种Dr. GRPO配置）。

### 4. 资源与算力

*   文中提到了部分训练和推理相关的算力细节，但**并未系统性地说明整个项目使用的总GPU型号、数量或总训练时长**。
    *   **推理/采样服务器**：Dr. GRPO阶段的轨迹采样明确使用了**H200实例**搭配 **vLLM 0.9.2**。
    *   **训练配置**：SFT阶段使用了 **LoRA** 进行微调，并提及了使用SDPA注意力机制和梯度检查点。Dr. GRPO训练阶段提及约**20步的训练过程**受到**12小时墙钟时间**的限制。
    *   **总体来看，论文缺乏对总计算资源消耗的明确统计。**

### 5. 实验数量与充分性

*   **实验数量**：
    *   报告了从基础模型到最终SFT栈的**多个中间检查点的性能**，构成了清晰的消融分析。
    *   对**KTO**进行了两个不同超参数（β值）的实验，并报告了一个负结果。
    *   对**Dr. GRPO**进行了三个变体实验，详细分析了从奖励不稳定到过程提升的过程。
    *   对比了近**10个**内部和外部模型/配置的性能。
*   **充分性与公平性**：
    *   **实验较为充分**。通过多阶段和多配置的对比，清晰地展示了各组件（过滤、拒绝采样、教师SFT等）的贡献。Dr. GRPO部分的消融实验尤其详尽，诊断了优化稳定性、奖励稀疏性等关键问题。
    *   **对比基本客观公平**。评估采用了防泄露的留出集和严格的评分标准，避免了数据泄露和评分宽松带来的虚假提升。直接与已发表的基线进行对比，并明确指出因评测工具集不同而需要谨慎解读的局限性，体现了客观性。
    *   **不足之处**：强化学习（Dr. GRPO）部分的实验并未完全收敛到能够弥补SFT模型与已发表SFT+GRPO标杆之间差距的程度，其结论是基于优化过程中的观察和趋势推断的，最终潜力尚未被完全验证。

### 6. 论文的主要结论与发现

*   **竞技场作为有效数据源的可行性**：激励对齐的智能体竞技场（如Bittensor SN15）能够作为一种全新的“轨迹原语”，持续、低成本地制造出具备多样性、逐过程监督和防泄露评估三大关键特性的后训练数据。
*   **蒸馏模型性能显著提升**：通过结构质量过滤器筛选出的少量高质量“智能体式”轨迹，对Qwen3-4B进行监督微调（SFT栈），就能将其在严格评估集上的ASR从**18.0%显著提升至42.7%**，接近仅用大量合成数据训练的SFT基线（43.6%）。
*   **Dr. GRPO奖励的作用**：SFT模型存在巨大的pass@k潜力（pass@8=53.3% vs pass@1=34.8%）。基于教师信号的、稠密的逐步**Dr. GRPO奖励能成功地将这种潜在能力转化为可测量的过程改进**（如幻觉减少、倾向正确决策），尽管未能在有限的训练步骤内完全转化为严格匹配上的精确成功率。
*   **数据结构是关键瓶颈**：确认竞技场产生的轨迹中，绝大部分（轴B）是“子任务式”的，模型仅在确定性的搜索循环上做分类和叙述，而真正有价值的“智能体式”轨迹（轴A）仅占极小部分。**将轴B洪流纳入训练管道是未来实现性能飞跃的主要杠杆。**

### 7. 优点：方法或实验设计上有哪些亮点

*   **新颖的数据生成范式**：将去中心化、激励对齐的竞赛系统直接视为一种带自动评判的数据生成器，为解决合成数据和日志数据的固有问题提供了新思路。
*   **严谨的评估体系**：引入“泄露簇”保护机制构建留出集，有效防止了因问题简单改写导致的过度乐观评估，确保了实验结果的可靠性。
*   **清晰且实用的数据过滤策略**：提出的结构质量过滤器不仅能过滤低质轨迹，更重要的是能准确区分“智能体式”和“子任务式”交互，这对于训练具备自主规划能力的智能体至关重要。
*   **深入的强化学习诊断**：Dr. GRPO部分的实验不仅是报告一个分数，而是通过对比多个变体，深入分析了奖励稀疏性、优化稳定性等问题，为后续研究指明了方向，这种过程分析很有价值。
*   **开源与可复现**：承诺公开过滤器代码、语料划分以及竞技场机制，促进了研究的可复现性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **数据利用极不充分**：模型训练仅使用了过滤后的一小部分“轴A”轨迹，而绝大部分“轴B”轨迹数据被直接丢弃，导致训练语料规模远小于对比的合成语料库。这是当前方法最主要的局限性。
*   **RL阶段未收敛到最优**：Dr. GRPO虽然在过程上有所提升，但并未将模型性能提升至与已发布的SFT+GRPO标杆（48.7%）相当的水平，其最终潜力仍待验证。
*   **评判信号的非金标准性**：作为过滤和训练关键信号的“推理系数”本身来源于另一个LLM的判断，其内在的偏见和错误模式可能被引入到训练数据中。
*   **评估可比性的局限**：由于本文使用了一个7工具的评测工具集，与原始ShoppingBench论文的4工具集不同，尽管作者指出了这一点，但与公开基准的直接数值对比仍需谨慎。
*   **计算成本描述不清晰**：未详细报告总体的算力消耗，难以评估该方法在经济成本上的优势或劣势。
*   **应用场景单一**：整个方法仅在ShoppingBench这一个购物智能体任务上进行了验证，其在其他类型智能体任务（如代码生成、网络浏览）上的泛化能力未知。

（完）
