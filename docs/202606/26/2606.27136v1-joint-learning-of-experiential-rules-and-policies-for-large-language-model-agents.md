---
title: Joint Learning of Experiential Rules and Policies for Large Language Model Agents
title_zh: 大语言模型智能体的经验规则与策略联合学习
authors: "Shicheng Ye, Chao Yu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27136v1"
tags: ["query:fla"]
score: 9.0
evidence: 从代理交互轨迹中联合更新经验规则和模型参数，一种直接的自主代理后训练技术
tldr: 大型语言模型智能体在多步交互任务中如何有效利用历史经验是一大挑战。常见做法或保留外部自然语言规则，或更新模型参数，但前者易与策略脱节，后者在稀疏奖励下难以纠正特定错误。本文提出联合学习经验规则与策略的JERP方法，同时维护规则池并优化策略：决策时检索匹配规则丰富上下文，互动后利用轨迹对比修订规则并提升策略。在AlfWorld和WebShop等复杂交互环境中，JERP带来稳定且一致的决策性能增益。这种方法使规则与策略同步进化，将可靠行为逐步内化到模型中，为构建自适应智能体提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27136v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1821, \"height\": 681, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 864, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 727, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1866, \"height\": 917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27136v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27136v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 385, \"label\": \"Table\"}]"
motivation: 现有LLM智能体在交互任务中利用经验时，外部规则存储与策略更新分离，导致规则与策略不一致，且参数更新难以纠正稀疏奖励下的特定错误。
method: JERP从交互轨迹同时更新经验规则池和策略，决策时检索任务相关规则增强提示，交互后通过轨迹对比修订规则并优化策略。
result: 在AlfWorld和WebShop等基准上，JERP相较基线在复杂交互任务中取得一致且显著的决策性能提升。
conclusion: JERP实现了经验规则与策略的协同学习，规则跟随策略进化并逐渐内化，为LLM智能体的持续适应提供了有效机制。
---

## 摘要
对于多步交互环境中的大语言模型智能体，一个关键挑战是如何有效利用积累的交互经验。现有工作通常将此类经验分为两种用途：将其作为自然语言规则保留在模型外部以供后续提示，或利用轨迹和反馈更新模型参数。前者易于解释但可能与不断演化的策略不同步；后者更广泛地改进了策略，但在稀疏奖励环境中对局部错误的纠正有限。我们提出了大语言模型智能体的经验规则与策略联合学习方法（JERP），它从相同的交互轨迹中更新长期经验规则池和策略。在决策时，JERP检索任务相关规则，并以此与交互历史共同作为智能体的条件。每个回合结束后，它利用收集的轨迹来优化策略，并通过将当前推演与参考成功轨迹进行比较来修正规则池。这种耦合使规则池与不断演化的策略保持一致，同时允许稳定有效的行为逐渐被模型自身吸收。在AlfWorld和WebShop上的实验表明，JERP在复杂交互任务的决策性能上取得了持续的提升。

## Abstract
For LLM agents in multi-step interactive environments, a key challenge is to make effective use of accumulated interaction experience. Existing work has typically separated two uses of such experience: keeping it outside the model as natural-language rules for later prompting, or using trajectories and feedback to update the model parameters. The former is easy to interpret but can fall out of sync with the evolving policy; the latter improves the policy more broadly but provides only limited correction for local mistakes in sparse-reward settings. We present Joint Learning of Experiential Rules and Policies for LLM Agents (JERP), which updates a long-term experiential-rule pool and the policy from the same interaction trajectories. At decision time, JERP retrieves task-relevant rules and conditions the agent on them together with the interaction history. After each episode, it uses the collected trajectories both to optimize the policy and to revise the rule pool by comparing current rollouts with reference successful trajectories. This coupling keeps the rule pool aligned with the evolving policy while allowing stable and effective behaviors to be gradually absorbed into the model itself. Experiments on AlfWorld and WebShop show that JERP yields consistent gains in decision performance for complex interactive tasks.

---

## 论文详细总结（自动生成）

好的，请稍等，我将基于您提供的论文内容，按照要求生成结构化的详细中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：对于在多步交互式环境中执行任务的大语言模型智能体，一个核心挑战是如何有效利用其积累的交互经验来持续提升决策能力。
*   **现有方法的局限**：当前的主流做法存在割裂，各有短板：
    *   **外部经验规则化**：将成功经验提炼为自然语言规则，存入外部记忆库，决策时作为提示词使用。这种方法便于解释，但规则是静态的，可能随着模型策略的更新而过时甚至产生误导。
    *   **模型参数更新**：通过强化学习等方法，直接用交互轨迹更新模型参数。这种方法能从全局层面改善策略，但在奖励信号稀疏的复杂任务中，难以对特定的局部错误进行及时、定向的纠正。
*   **整体含义**：本文旨在**弥合上述两种经验利用方式的鸿沟**，提出一种“联合学习”框架，让经验规则与模型策略在同一次交互数据驱动下协同进化，实现优势互补。

### 2. 论文提出的方法论

核心思想是 **联合学习经验规则与策略（JERP）**，其关键点在于一个“双重更新”的闭环训练流程。

*   **核心机制**：对同一个任务的同一批交互轨迹进行两次利用：
    1.  **策略优化（参数更新）**：用于更新智能体的神经网络参数。
    2.  **经验规则池更新（非参数更新）**：用于维护一个存储自然语言规则的长期记忆库。
*   **关键技术细节与算法流程**：
    *   **规则池与检索（决策阶段）**：每个任务维护一个经验规则池 `K(d)`，每条规则包含文本内容 `z` 和效用分数 `s`。决策时，根据分数排序，选取 Top-k 个规则作为**工作规则集** `eK(d)`，与任务描述、当前交互历史一同构成提示词输入模型，生成动作。
    *   **阶段一：模型参数更新**：采用基于组相对优势的策略优化目标（类似于GRPO [26]）：
        1.  对同一任务采样 N 条完整轨迹。
        2.  计算每条轨迹的总奖励，并据此在组内计算相对优势 `A_i`。
        3.  优化目标是最大化优势加权下的动作似然，并通过剪切（PPO-Clip）和与参考模型的KL散度进行正则化，防止策略发生剧烈变化。
    *   **阶段二：经验规则池更新**：通过**对比反思（Contrastive Reflection）** 实现规则的动态维护。利用一个参数冻结的LLM：
        1.  **输入**：当前规则池 `K(d)`、刚采样的轨迹组 `T_d`、该任务历史上积累的**参考成功轨迹集** `T+(d)`。
        2.  **对比分析**：模型对比当前行为与成功轨迹，生成结构化的“规则编辑操作列表” `O(d)`。
        3.  **操作类型**：包含五种原子操作，实现对规则池的增、改、强化、弱化、合并。
            *   `ADD (z)`: 添加新规则。
            *   `EDIT (q, z)`: 修改现有规则文本。
            *   `UPVOTE (q)` / `DOWNVOTE (q)`: 提升/降低规则的效用分数。
            *   `MERGE (Q, z)`: 将语义或功能重叠的多条规则合并为一条。
        4.  **规则生命周期管理**：新规则赋予初始分，修改不影响分数，投票增减固定值，合并规则取最高分。分数低于阈值的规则将被剔除，控制规则池规模。

### 3. 实验设计

*   **数据集/场景**：
    *   **AlfWorld**：基于文本的家务模拟环境，包含6种多步任务（如“拿起并清洁后放置”），环境部分可观测。
    *   **WebShop**：在线购物模拟环境，智能体需进行搜索、浏览、比较和购买等操作。
*   **评估指标**：
    *   AlfWorld：任务成功率（%）。
    *   WebShop：平均得分（Score）和任务成功率（%）。
*   **对比方法**：
    *   **未训练或仅推理的方法**：`Vanilla LLM` (直接提示), `ReAct` (基于上下文推理但无跨轮次记忆), `Reflexion` (基于外部记忆的反思，无参数更新)。
    *   **仅参数更新的方法**：`RLOO` (基于REINFORCE的风格优化), `GRPO` (组相对策略优化)。
    *   **JERP**：本文提出的联合学习方法，所有需训练的方法均使用LoRA进行参数高效微调。

### 4. 资源与算力

*   **硬件平台**：服务器配备了**4张NVIDIA A30 GPU**，约512 GB内存。
*   **训练细节**：论文未明确给出总训练时长（小时/天）。但提供了关键超参数，如AlfWorld训练200轮（epochs），WebShop训练300轮。GRPO和JERP都采用了LoRA技术降低内存和计算开销。

### 5. 实验数量与充分性

*   **实验分组**：
    1.  **主实验**：在两个不同的交互式基准（AlfWorld, WebShop）上与5种基线方法进行全面对比。
    2.  **训练曲线分析**：绘制了JERP和主要基线（GRPO, RLOO）在AlfWorld不同任务上的成功率曲线，展示了学习动态。
    3.  **消融实验**：为验证经验规则池动态更新的必要性，设置了“规则池更新暂停”的消融变体，并与完整JERP进行对比。
*   **充分性与客观性**：
    *   **充分**：实验覆盖了不同类型的基准环境和基线，能清晰证明方法的有效性。消融实验直接验证了核心组件的贡献。
    *   **客观公平**：所有需训练的基线均采用相同的LoRA微调设置和硬件平台，结果报告为3次独立运行的平均值，减少了随机性偏差，比较是客观公平的。

### 6. 论文的主要结论与发现

*   **性能提升**：JERP在两个基准上均取得了最优的总体性能。在AlfWorld上的成功率（61.5%）显著优于最强的纯参数更新基线GRPO（57.8%），在WebShop上也取得了最高的平均分和成功率。
*   **优势场景**：JERP在步骤更长、中间约束更多的任务（如AlfWorld的 `Clean`, `Heat`, `Cool`, `Pick2`）上优势更为明显。这说明动态更新的规则池能有效沉淀早期交互中发现的、可复用的操作模式。
*   **协同效应**：联合学习框架让规则池能跟随策略一同进化。策略参数提供主要的改进动力，而规则池则负责保留和纠正那些在参数层面难以快速解决的、任务特定的局部行为模式。
*   **机制有效性**：消融实验证实，持续的规则池更新对性能提升至关重要。若在训练初期固定规则池，后续的性能增长会显著放缓。

### 7. 优点
*   **创新的联合框架**：首次将经验规则的动态维护与策略的参数优化紧密结合在一个训练循环中，实现两种经验利用方式的优势互补。
*   **精细化的规则管理**：提出了包含增、改、投票、合并等多种操作的规则池动态维护机制，使得经验知识既能不断积累新知识，又能修正错误、淘汰冗余信息。
*   **可解释性与自适应性**：通过自然语言规则的形式保留了经验的可解释性，同时允许规则随策略自适应演化，解决了静态规则僵化的问题。

### 8. 不足与局限
*   **计算开销较大**：每个交互轮次结束后都需要调用一个额外的LLM进行对比反思和规则编辑，增加了训练时间和计算成本。论文未对比此方法在训练效率上与纯参数更新方法的差异。
*   **规则检索策略相对简单**：当前仅基于规则自身的效用分数进行Top-k检索，未实现更细粒度的、基于当前情境与规则语义相似度的实例级检索，可能影响规则的精准应用。
*   **任务通用性待验证**：实验主要在特定类型的模拟环境（家庭、购物）中进行，其在更广泛、更具创造性的生成式任务或多模态交互任务上的有效性仍有待探索。
*   **超参数敏感性**：框架引入了多个新增超参数（如规则分数增减量、初始分数、合并策略等），其对不同任务的泛化能力和敏感性未做深入分析。

（完）
