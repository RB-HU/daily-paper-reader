---
title: Effective Reinforcement Learning for Agentic Search by Recycling Zero-Variance Queries During Training
title_zh: 通过训练期间回收零方差查询实现智能体搜索的有效强化学习
authors: "João Coelho, João Magalhães, Bruno Martins, Chenyan Xiong"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10709v1"
tags: ["query:fla"]
score: 8.0
evidence: 针对智能体搜索RL训练中的零方差查询，提出查询回收策略以提升训练效率
tldr: "在训练LLM搜索代理时，GRPO风格算法仅利用包含成功与失败混合的样本组，全正确或全错误组被视为零方差而浪费资源。现有方法静态丢弃这些组，但我们发现查询的方差状态会随策略演化而动态变化。为此，提出查询回收技术，将零方差样本组放回可复用池，使其在未来训练中适时重采样，实现训练分布与策略协同进化。在合成数据上训练的1.7B模型于七个多跳QA基准平均Pass@1达66.0，匹配或超越7B监督模型，且回收查询贡献了训练后期约四分之三的有效批次。该技术显著提升训练效率和小模型性能，揭示了零方差查询的动态价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10709v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 769, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 797, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 790, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 799, \"height\": 361, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 800, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 799, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1549, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10709v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1658, \"height\": 1247, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10709v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1667, \"height\": 892, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10709v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1602, \"height\": 1006, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10709v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 811, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10709v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1656, \"height\": 444, \"label\": \"Table\"}]"
motivation: 现有方法静态丢弃零方差查询，造成训练样本浪费，且未利用查询方差状态随策略演化的动态特性。
method: 提出查询回收：将零方差查询放回可变池，在后续训练中根据策略变化重新采样，让训练分布自适应调整。
result: "1.7B模型平均Pass@1达66.0，性能比肩或超越7B监督模型；回收查询在训练末尾贡献约四分之三的有效批次。"
conclusion: 查询回收通过动态复用零方差样本，大幅提升小模型的训练效率与最终性能，证明零方差查询可随时间转化为有价值的信号。
---

## 摘要
采用 GRPO 风格算法已成为在仅有结果奖励的情况下训练 LLM 搜索智能体的标准策略。在这些算法中，只有当查询的展开组同时包含成功和失败时，才对参数更新有贡献；全部正确（过于简单）和全部错误（过于困难）的组是零方差的，浪费了展开成本。现有方法将零方差视为静态属性，并丢弃或预过滤这类组。我们假设并通过实验验证，随着策略在训练过程中的演进，查询会在零方差和承载信号状态之间翻转。基于这一直觉，我们提出了查询回收机制，将零方差组返回到可变池中以供未来重新采样，从而使有效训练分布与策略共同演化。采用所提出的技术，一个在合成数据上训练的 17 亿参数模型在七个多跳问答基准上达到了平均 66.0 的 Pass@1 分数，与在基准衍生监督下训练的多达 70 亿参数的模型相当或更优。对回收模式的分析表明，到训练结束时，回收查询贡献了约四分之三的有效批次，其贡献来自策略改进和策略漂移的恢复。

## Abstract
The use of GRPO-style algorithms has become the standard strategy for training LLM search agents under outcome-only rewards. With these algorithms, a query contributes to parameter updates only when its rollout group mixes successes and failures; all-correct (too-easy) and all-incorrect (too-hard) groups are zero-variance and waste rollout cost. Existing approaches treat zero-variance as a static property and either discard or pre-filter such groups. We hypothesize and empirically validate that queries flip between zero-variance and signal-bearing states as the policy evolves during training. Building on this intuition, we propose query recycling, which returns zero-variance groups to a mutable pool for future resampling, so that the effective training distribution co-evolves with the policy. With the proposed technique, a 1.7B parameter model trained on synthetic data can reach 66.0 average Pass@1 accross seven multi-hop QA benchmarks, matching or surpassing systems with up to 7B parameters trained on benchmark-derived supervision. Analysis of recycling patterns shows that recycled queries supply roughly three quarters of the effective batch by the end of training, with contributions split between recovery from policy improvement and policy drift.

---

## 论文详细总结（自动生成）

好的，这是根据提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

-   **研究背景与动机：**
    -   当前，使用GRPO风格的强化学习算法训练LLM搜索智能体已成为主流。在仅有结果奖励（正确/错误）的设置下，算法仅从包含成功与失败样本的“有信号”查询组中学习。
    -   “零方差”查询组（即所有展开结果都正确或都错误）的梯度为零，无法提供学习信号，但仍消耗了同样的模型展开计算资源。
    -   现有工作（如DAPO, ASearcher, GRESO）将“零方差”视为查询的**静态属性**，会直接丢弃或提前过滤掉这些查询组，假设它们永远无法提供学习信号。

-   **整体含义与核心假设：**
    -   本文作者提出并验证了一个相反的假设：**零方差是查询在特定策略下的瞬时状态**，而非固定属性。随着模型策略在训练中演进，一个“过于困难”的查询可能变得可解，而一个“过于简单”的查询可能因策略漂移而重新变得有挑战性。
    -   基于此，论文提出了一种名为**查询回收**的新训练策略，动态管理训练查询池，让零方差查询有机会在未来的训练步骤中被重新采样，从而提升训练效率和最终模型性能。

### 2. 论文提出的方法论

-   **核心思想：动态池管理与查询回收**
    -   将训练池形式化为一个带权重的查询集合。权重代表该查询被采样的概率。
    -   在训练的每一步，根据权重采样一组查询并生成展开结果，然后根据该组是否产生梯度信号来**更新查询权重**。
    -   **查询回收**就是具体的权重更新规则：将已产生学习信号的查询（有信号组）权重**置零**（即从池中移除），而将未能产生信号的零方差查询权重**保留或重置为1**（即可被回收），使其可以在未来策略下被重新采样。

-   **关键技术细节与算法流程：**
    1.  **初始化训练池**：包含N个带权重的查询，所有查询的初始采样概率均为1。
    2.  **采样候选集**：在每个训练步，根据概率采样一个大小为 \(k \cdot B\) 的候选查询集 \(C_t\)（其中 \(B\) 为批次大小，\(k\) 为过采样因子，用于增加发现信号组的概率）。
    3.  **展开与筛选**：为每个候选查询生成 \(K\) 个展开结果，并根据成功/失败情况筛选出有信号组 \(\tilde{S}_t\)（即包含混合结果的组）。
    4.  **梯度更新**：从 \(\tilde{S}_t\) 中选取最多 \(B\) 个查询形成有效批次 \(S_t\) 进行梯度更新。
    5.  **权重更新（查询回收）**：将被选入有效批次 \(S_t\) 的查询权重置零（`w = 0`），而未产生信号的查询（零方差组）权重保持不变（`w = 1`），等待未来回收。
    6.  **与传统方法对比**：该框架也统一了标准GRPO（`k=1`，消耗所有采样查询）和DAPO（动态增大`k`直到凑满有信号批次，并消耗所有采样查询）。

### 3. 实验设计

-   **训练数据集/场景：**
    -   **完全合成且沙盒化**：训练基于ClueWeb22语料库的静态快照，使用DeepResearchGym作为可复现的检索环境，不依赖任何评估基准的训练集。
    -   **合成查询池构建**：共2.5万条查询-答案对。由DeepResearchGym的9千条查询和额外1.6万条查询构成。后者通过三种方法生成，以诱导不同的搜索策略：
        -   **网络图谱查询**：基于超链接的跳转链。
        -   **迭代式搜索查询**：基于密集检索的链式提问。
        -   **比较式搜索查询**：需要并行检索多个独立实体。
    -   查询池被分为1.5万条用于监督微调，1万条用于强化学习。

-   **评估基准：**
    -   **用于对比的主要基准**：七个多跳问答基准——2WikiMultiHopQA, Bamboogle, HotpotQA, MuSiQue, Natural Questions, PopQA, TriviaQA。使用GPT-4.1-mini作为评判器计算Pass@1指标。
    -   **长程推理基准**：GAIA， HLE， WebWalkerQA。
    -   **推理设置**：推理时使用SERPER实时网络搜索引擎，验证策略从沙盒环境到真实网络的迁移能力。

-   **对比方法：**
    -   **核心对比实验**：在**匹配的展开预算**下，对比了三种基于GRPO的方法：
        -   **标准GRPO**：`k=1`，多轮遍历训练池。
        -   **有界DAPO**：`k=2`，不进行回收。
        -   **有界DAPO+查询回收**：`k=2`，采用本文提出的回收策略。
    -   **外部基准对比**：将最佳模型与多个现有系统进行比较，包括SearchR1, DeepResearcher, R1Searcher, BehaviorPrime, ASearcher-Web-7B， ORBIT, SmartSearch。

### 4. 资源与算力

-   论文中**未明确提及**具体的GPU型号、数量或详细的训练时长。
-   文中仅指出了模型规模（Qwen3-1.7B和Qwen3-4B）、训练批次大小（`B = 32`）、组大小（`K = 4`）和总展开轨迹预算（84.5K条）。所有对比方法都在这个**相同的展开预算**下进行，确保了公平比较。

### 5. 实验数量与充分性

-   **实验数量充足，分析深入**：
    -   **主要对比实验**：在两个模型规模（1.7B和4B）上，对3种训练策略进行了全量微调和评估，这是共 **6组主要实验**。
    -   **消融与分析实验**：对回收机制进行了深入分析，包括：
        1.  追踪有效批次中新鲜查询与回收查询的构成比例变化（Figure 4）。
        2.  分析训练池中不同难度查询比例的演变（Figure 5）。
        3.  对比不同组大小（`K=4`和`K=8`）对回收效果的影响（Figure 6）。
        4.  分析了不同查询生成来源（如迭代式、图谱式）在训练中的动态表现（附录中的Figure 7, 8）。
        5.  探索了“软回收”策略，即不完全移除有信号查询，而是将其权重降低（附录中的Table 4）。
-   **实验的客观性与公平性**：
    -   **公平性高**：所有RL对比实验都从同一个SFT检查点开始，且在完全**匹配的展开预算**（84.5K条）下进行，这是最核心的公平性保证。
    -   **客观性**：通过在完全合成的数据上训练，并在多个分布外、未见过的基准上进行评估，有效避免了数据污染，增强了结论的客观性与泛化能力。

### 6. 论文的主要结论与发现

-   **性能显著提升**：通过查询回收，一个在合成数据上训练的1.7B模型在七个多跳QA基准上的平均Pass@1达到**66.0**，匹配甚至超越了使用基准衍生监督数据训练的多达7B参数的系统。4B模型性能进一步提升至**71.3**。
-   **回收查询是后期训练的主导信号**：在训练后期，回收查询（由“过度”和“过难”恢复而来）贡献了**约四分之三**的有效训练批次，证明了零方差查询动态再利用的巨大价值。
-   **零方差状态的策略相对性**：实验证实了核心假设，即零方差状态是动态的、依赖于当前策略的。随着训练进行，4B模型能将近一半的查询池转化为“过于简单”状态，而1.7B模型则保留了大部分“过于困难”的查询，反映了不同容量模型的策略演进差异。
-   **信号来源的双重性**：回收信号不仅来自因**策略改进**而从“过难”状态恢复的查询，也大量来自因**策略漂移**而从“过易”状态跌落的查询，后者占比约40%，不容忽视。

### 7. 优点

-   **巧妙的视角与低成本方法**：揭示了零方差查询的动态本质，并提出了一个无需复杂奖励设计、不引入额外模型的简单而高效的训练策略，即权重管理。
-   **训练效率高，环境友好**：通过在相同计算预算下更多地利用有效信息，显著提升了小模型性能，用小模型匹配甚至超越更大模型。同时，完全基于合成数据和沙盒环境训练，避免了昂贵且不可复现的实时API调用。
-   **实现干净，分析透彻**：将多种现有方法统一到“动态池管理”框架下，概念清晰。实验设计公平（匹配预算），分析维度丰富（来源、规模、组大小），对回收信号的双重来源（改进和漂移）的剖析尤其深刻。

### 8. 不足与局限

-   **评估可复现性**：使用LLM作为评判器和实时网络搜索（SERPER）进行评估，这继承了该方法固有的可复现性问题。
-   **语言和领域局限性**：实验主要集中在英语的多跳问答基准上，合成查询池也基于英文语料库。其在多语言或跨领域任务上的表现未经检验。
-   **模型规模的泛化性**：结论在Qwen3-1.7B和4B模型上得出。论文承认，在更大规模模型或其他模型家族上，“过易”与“过难”查询回收的比例可能发生变化，其效果有待验证。
-   **固定训练池的限制**：查询回收受限于初始的固定训练池规模。随着策略不断改善，池中剩余的“有信号”查询会逐渐耗尽，训练可能会触及瓶颈。论文提到在线生成新查询是未来的方向。
-   **奖励形式单一**：当前实验仅基于结果奖励。虽然查询回收在理论上与过程奖励等更密集的监督信号正交，但文中未对此组合进行验证。

（完）
