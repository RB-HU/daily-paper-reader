---
title: "DiDPO: Diff-in-Diff Policy Optimization for Coding Agent Training"
title_zh: DiDPO：面向编码智能体训练的差分差分策略优化
authors: "Xucong Wang, Zhe Zhao, Liheng Yu, Di Wu, Xiaofeng Cao, Pengkun Wang"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07147v1"
tags: ["query:fla"]
score: 9.0
evidence: 基于差异的RL后训练编智能体信用分配
tldr: "现有RLVR方法编码智能体信用分配粗粒度，难分辨代码差异独立贡献。DiDPO直接从代码差异结构构建细粒度信用，用可分组分数分割差异形成优势组，将优势投影到令牌。在编码推理基准上，DiDPO显著优于基线，在Qwen2.5-7B-Coder上超过10%，缩小与更大模型差距。提供了细粒度信用分配原则性框架并开源代码库。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07147v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07147v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 826, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07147v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 874, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07147v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 824, \"height\": 325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07147v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 825, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07147v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1699, \"height\": 387, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07147v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 965, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07147v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1657, \"height\": 978, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07147v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 808, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07147v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1653, \"height\": 327, \"label\": \"Table\"}]"
motivation: 现有RLVR方法对编码智能体信用分配粗粒度，无法区分代码差异中独立更改的贡献。
method: DiDPO是无critic的RL方法，通过可分组分数分割代码差异为子差异，聚合相似子差异形成锚点及优势组，将差异级优势投影回令牌。
result: "在长时域编码和推理基准上，DiDPO显著优于强基线，在Qwen2.5-7B-Coder上超过可比方法10%以上，缩小与更大模型差距。"
conclusion: DiDPO提供了细粒度信用分配的原则性框架，有效提升编码智能体性能，并开源了verl-code代码库。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为训练编码智能体的高效范式，其中编译和测试产生的执行反馈提供了客观的验证信号。然而，与一般智能体任务不同，编码智能体面临着一个独特且更细粒度的贡献分配挑战：在每一步中，编码动作会同时将不同的修改打包到代码版本的不同区域，这使得独立修改的贡献难以区分。现有的RLVR方法大多利用结果奖励或步骤级奖励，未能深入代码差异，导致编码动作的独特属性在训练中被忽视。本文提出差分差分策略优化（DiDPO），一种无需价值网络的强化学习方法，直接从代码差异的结构中构建细粒度的贡献单元。DiDPO将多轮编码互动组织为多个“思考—动作”步骤，并在采样的轨迹中发现代码差异。然后，通过我们提出的“可分组性评分”，将每个完整差异拆分为高度相似的子差异，并以此聚合锚点。该评分提供了一种拆分方案，能够最优地平衡锚点的语义范围与其可能形成的组规模。最终，这些锚点形成优势组，并将差异级别的优势投影回单个响应标记。在长期编码和推理基准上的实验表明，DiDPO显著优于强大的智能体强化学习基线。在Qwen2.5-7B-Coder模型上，DiDPO超过同类方法超过10%，并缩小了与更大模型的差距，为编码智能体训练中的细粒度贡献分配提供了一个原则性框架。我们还开源了verl-code，一个支持多种强化学习方法和编码基准的智能体强化学习代码库。

## Abstract
Reinforcement learning with Verifiable Reward (RLVR) has emerged as a powerful paradigm for training coding agents, where the execution feedback from compilation and tests provides objective verification. However, unlike agent tasks, coding agents face a unique and finer-grained credit assignment challenge: at each step, coding actions simultaneously pack varying changes into different regions of a code version, which makes the contribution of independent change indistinguishable. Existing RLVR methods mostly leverage the outcome reward or step-level reward, which fails to dive into a code diff and makes unique properties of coding actions invisible to training. In this paper, we propose Diff-in-Diff Policy Optimization (DiDPO), a critic-free RL method that constructs fine-grained credit units directly from the structure of code diffs. DiDPO organizes multi-turn coding interactions into multiple thought--action steps and discovers code diffs across sampled trajectories. It then selects anchors by aggregating highly similar sub-diffs split from each whole diff by our ``groupability score'', which provides the splitting schema that optimally balances the semantic scope of anchors and the group mass they may form. Finally these anchors form advantage groups and project the diff-level advantage back to individual response tokens. Experiments on long-horizon coding and reasoning benchmarks show that DiDPO significantly outperforms strong agentic RL baselines. On Qwen2.5-7B-Coder, DiDPO exceeds comparable methods by over 10\% and narrows the gap with far larger models, offering a principled framework for fine-grained credit assignment in coding agent training. We also open-source verl-code, an agentic rl codebase that supports various RL methods and coding benchmarks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：在训练编码智能体时，现有的基于可验证奖励的强化学习（RLVR）方法进行贡献分配（credit assignment）的粒度太粗。
    -   **具体表现**：智能体在每一步中输出的代码修改（code diff）通常包含多个功能独立的子修改（sub-diff），这些修改作用于代码的不同区域，且对最终执行结果的贡献各不相同。
    -   **现有方法的缺陷**：无论是基于最终结局奖励（outcome reward）还是基于步骤奖励（step-level reward）的方法，都将整个 diff 或整个动作视为一个原子单元，无法区分其中独立且功能各异的子修改的贡献。
    -   **整体含义**：论文旨在提出一种全新的、更细粒度的贡献分配方法，该方法直接利用代码 diff 的内在层次结构，为编码智能体训练提供更精确、更有效的学习信号。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

DiDPO 是一种无需价值网络（critic-free）的强化学习方法，其核心思想是将粗粒度的代码 diff 拆解为更细粒度的“子差异”，并通过跨轨迹对比，为这些细粒度单元构建优势函数。

-   **核心思想与流程**：
    1.  **Diff 拆解与发现**：将多轮编码交互中的所有 diff 聚合，并进一步枚举每个 diff 的所有连续子差异（sub-diff）作为候选。
    2.  **相似性匹配与锚点选择**：计算不同轨迹中、属于同种编辑类型（如均为 `add`）的子差异对之间的相似度。通过作者提出的 **“可分组性评分（Groupability Score, GS）”**，从候选子差异中选择最优的**锚点（anchor）**。该评分同时考虑锚点自身的语义范围（大小）和其能聚合的元素数量（组质量），旨在平衡二者的关系，避免选择过小、无意义的片段或过大的组。该选择过程被形式化为一个受基数约束的子模最大化问题，可通过贪心算法求解。
    3.  **优势组构建**：根据选定的锚点，将相似的子差异归入同一个**优势组（advantage group）**。
    4.  **多级别优势计算与投影**：
        -   在每个优势组内部，计算**差异级优势（diff-level advantage, A_D）**，用于衡量该子差异相对于组内其他同种子差异的“好坏”。
        -   同时计算传统的**轨迹级优势（trajectory-level advantage, A_E）**。
        -   将两者加权求和，得到一个最终的**令牌级优势（token-level advantage）**，并投影回该子差异对应的各个响应令牌上，用于策略梯度更新。整个过程无需额外的环境交互或价值网络。

-   **关键公式说明**：
    -   **可分组性评分 (GS)**：`GS(c) = (1 - e^{-L̄(c)}) (1 - e^{-(n(c)-1)})`。其中 `c` 为锚点候选，`L̄(c)` 是其平均大小，`n(c)` 是其出现次数。此公式确保了只有当规模和组质量都较大时，锚点才能获得高分。
    -   **锚点选择目标**：在基数 `K` 的限制下，选择一组锚点 `C*`，最大化所有子差异被其“最佳”锚点覆盖的GS值之和。
    -   **最终优势**：`Â_{i,l} = A_E(τ^{(i)}) + λ · A_D(a^{(i)}_l)`。将轨迹级优势与差异级优势结合，赋予每个响应令牌。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

-   **数据与场景**：实验专注于**长期编码与推理任务（long-horizon coding and reasoning）**。
-   **基准测试（Benchmarks）**：覆盖了功能级和竞赛级的多种评估集。
    -   **标准代码生成**：APPS (分难度)、HumanEval、MBPP、LiveCodeBench、LeetCode。
    -   **竞赛级算法**：USACO (分金银铜铂金)、OJBench (分难度)、ICPC。
-   **对比方法（Baselines）**：与三组模型/方法进行了全面比较。
    -   **大型参考模型**：Kimi-K2.6, Qwen3.6-27B, GLM-5.2, GPT-5.5, Skywork-OR1 等。
    -   **代码推理基线**：CoT（思维链）、CodeAct、Self-Planning。
    -   **编码/智能体强化学习基线**：在 SFT 基础上分别应用 CodeRL+、GRPO、GiGPO 及 DiDPO 自身。
-   **骨干模型（Backbones）**：在两个不同规模的开源模型上进行实验：`Qwen2.5-Coder-7B` 和 `Qwen3.5-4B`，并在RL训练前均进行了一轮监督微调（SFT）以对齐“思考-动作”格式。

### 4. 资源与算力

-   **硬件配置**：训练使用 **1 个节点，配备 8 张 H20 GPU**。
-   **并行策略**：张量并行度为 2。
-   **训练时长**：DiDPO 训练阶段进行了 **120 步**。相对于 GRPO，DiDPO 每步训练时间的额外开销仅为 **~2.3%**，主要由跨轨迹的子差异相似度计算产生。

### 5. 实验数量与充分性

-   **实验组数**：实验设计全面且充分。
    -   **主实验**：在两个不同规模的骨干模型上，在超过8个标准及竞赛级代码基准上进行了性能评估。
    -   **消融实验**：
        -   验证了 `A_E` 和 `A_D` 两个优势信号组件的必要性，以及“子差异分解”这一设计的作用。
        -   对比了不同 **“可分组性评分 (GS)”** 设计方案的效果，包括线性组合和用大模型作为裁判。
    -   **敏感性分析**：分析了关键超参数 `λ` 对性能的影响。
    -   **动态分析**：展示了训练过程中组类型的演变以及组质量分布的变化。
    -   **效率分析**：对比了 DiDPO 与 GRPO 的训练时间开销。
-   **客观性与公平性**：所有可训练的对比方法均使用相同的任务分布、奖励信号、交互格式和评估协议，并在必要时统一从相同的 SFT 检查点开始训练，确保了比较的客观与公平。

### 6. 论文的主要结论与发现

-   **性能显著提升**：DiDPO 在所有基准上一致地、大幅度地超越了所有同级别的智能体强化学习基线。
    -   在 `Qwen2.5-Coder-7B` 上，平均性能超越最强基线 **GiGPO 4.2 个百分点**，超越 GRPO **5.6 个百分点**，在 APPS Interview 难度上甚至领先 10.4 个百分点。
    -   在两个骨干模型上，都显著缩小了与 GPT-5.5 等大型模型的差距。
-   **细粒度贡献分配的关键作用**：消融实验证明，移除差异级优势（`A_D`）或取消子差异分解都会导致性能的显著下降，证实了该方法设计中各组件，尤其是细粒度信用分配机制的有效性。
-   **可分组性评分的有效性**：提出的 `GS` 公式优于简单的线性组合或使用大模型评判，能够更好地平衡锚点的语义完整性与组的规模。
-   **方法的高效性**：DiDPO 在引入更精确学习信号的同时，只带来了极小的额外计算开销。

### 7. 优点：方法或实验设计上有哪些亮点

-   **创新性强**：首次将关注点深入到代码智能体动作的内部结构，利用代码 diff 的层次性，将贡献分配从“动作/步骤”级推进到“子修改”级。
-   **理论支撑**：提供了理论分析，从偏差-方差权衡的角度解释了“可分组性评分”的工作机理，证明了更优的锚点对齐能降低偏差，组内平均能降低方差。
-   **方法高效实用**：所有组信息直接从模型自身的采样轨迹中提取，**无需额外的环境交互或训练价值网络**，且增加的训练开销极小（~2.3%）。
-   **评估全面有力**：在合成任务、竞赛题、仓库级修复等多种场景下，与多种基线（提示工程、现有RL方法）进行了充分对比，结合消融实验和敏感性分析，结论非常扎实。
-   **开源贡献**：开源了 `verl-code` 代码库，为社区提供了支持多种RL方法和编码基准的智能体训练框架。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

-   **骨干模型单一**：方法仅在 Qwen 系列的 4B 和 7B 模型上验证，其在更大规模模型（如 70B+）或不同架构（非 Qwen）的编码模型上的效果有待考证。
-   **任务类型局限**：目前仅聚焦于文本代码生成。虽然理论上能处理任何产生结构化差异的输出，但其在视觉-语言行为智能体等其他领域的通用性尚未被证实。
-   **计算开销仍有存在**：虽然比例很小，但跨轨迹的子差异相似度计算复杂度仍可能随着候选子差异和轨迹数量的增加而上升，在更大规模或更长轨迹的场景下可能成为瓶颈。
-   **超参数依赖**：方法引入了新的超参数，如差异级优势权重 `λ` 和相似度阈值 `η`，这些参数可能需要根据具体任务进行调整。论文中 `λ` 的敏感度分析表明性能对此参数敏感。
-   **评估的静态性**：评估基于静态的基准测试，未在实时、持续演变的软件项目中验证其在分布外或对抗性场景下的鲁棒性。

（完）
