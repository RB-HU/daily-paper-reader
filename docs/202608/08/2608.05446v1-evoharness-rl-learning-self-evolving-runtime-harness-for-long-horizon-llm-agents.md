---
title: "EvoHarness-RL: Learning Self-Evolving Runtime Harness for Long-Horizon LLM Agents"
title_zh: EvoHarness-RL：为长程大语言模型智能体学习自演进运行时框架
authors: "Xuying Ning, Dongqi Fu, Tianxin Wei, Hanqing Zeng, Yuanchen Bei, Bingxuan Li, Zihao Li, Qifan Wang, Xiang Shen, Yifan Wu, Jiayi Liu, Hong Li, Yinglong Xia, Xiangjun Fan, Hanghang Tong, Jingrui He"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.05446v1"
tags: ["query:fla"]
score: 9.0
evidence: 通过强化学习离线学习控制策略，用于智能体在线状态管理，是自主智能体后训练的直接技术
tldr: "长程LLM智能体依赖外部状态管理，但从嘈杂交互轨迹构建状态和运行时控制访问极具挑战，现有手动设计方法缺乏适应性。EvoHarness-RL引入可训练Harness策略，通过监督微调学习构建Belief、Progress、Experience（BPE）状态，并利用成本感知GRPO探索离线协调策略，实现运行时选择性读取、更新与巩固。在ALFWorld上基于Qwen3-8B达96.9%成功率，并揭示Harness退火（内化使用模式，减少频繁调用）和Harness进化（状态细化至紧凑任务基底）。研究表明，长程智能体需要学习构建和协调外部Harness工作区的策略，而不仅依赖更强工具或更大记忆。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05446v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1714, \"height\": 635, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05446v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1726, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05446v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 729, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05446v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 732, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05446v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 692, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05446v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 663, \"height\": 455, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05446v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 1164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05446v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 519, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05446v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1706, \"height\": 371, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05446v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1613, \"height\": 1660, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05446v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1289, \"height\": 1479, \"label\": \"Table\"}]"
motivation: 长程LLM智能体依赖外部状态管理，但手动设计的状态构建与访问策略难以从噪声交互中自适应学习。
method: 提出EvoHarness-RL，通过监督微调教会智能体构建BPE外部状态，并利用成本感知GRPO学习选择性读取、更新与巩固的策略。
result: "在ALFWorld上基于Qwen3-8B，成功率达96.9%，并观察到Harness退火（内化使用模式）和进化（状态紧凑化）。"
conclusion: 研究表明，长程智能体需要学习构建和协调外部Harness工作区的策略，而不仅依赖更强工具或更大记忆。
---

## 摘要
长程大语言模型智能体越来越依赖外部执行支持来维护状态、跟踪进度、调用工具、验证结果以及跨交互重用经验。然而，有效的框架使用带来了两个耦合的挑战：从嘈杂的交互轨迹中形成状态，以及对外部状态访问的运行时控制。现有智能体通常通过提示、启发式或领域特定约定来处理这两者，使得外部工作空间及其使用策略由人工设计。为了解决这个问题，我们研究了框架策略学习问题，其中智能体离线学习框架策略，并在运行时任务执行期间在线部署它们以构建和更新外部框架状态。我们引入EvoHarness-RL，它将信念、进度和经验（BPE）作为面向策略的框架状态暴露出来。有监督的框架微调教会基础智能体框架动作空间以及如何构建有用的外部状态，而成本感知的GRPO则探索协调策略，以在长程交互中选择性地读取、更新和整合该状态。在ALFWorld上基于Qwen3-8B大语言模型实例化，EvoHarness-RL达到了96.9%的成功率，并揭示了两个关键动态：框架退火，其中训练将重复的框架使用模式内化到模型策略中，使智能体从频繁的框架调用转向选择性的外部状态访问；以及框架演进，其中进度更新和经验整合将框架提炼成一个紧凑的、任务自适应的状态基板。这些结果表明，长程智能体受益于用于构建外部框架工作空间并与之协调的可训练策略，而不仅仅是添加更强大的工具或更大的记忆。

## Abstract
Long-horizon LLM agents increasingly rely on external execution support to maintain state, track progress, invoke tools, verify outcomes, and reuse experience across interactions. However, effective harness use raises two coupled challenges: state formation from noisy interaction traces and runtime control over external-state access. Existing agents usually handle both through prompts, heuristics, or domain-specific conventions, leaving the external workspace and its usage policy manually engineered. To address this, we study the problem of harness policy learning, where agents learn harness policies offline and deploy them to construct and update external harness state online during runtime task execution. We introduce EvoHarness-RL, which exposes Belief, Progress, and Experience (BPE) as policy-facing harness state. Supervised harness fine-tuning teaches the base agent the harness action space and how to construct useful external state, while cost-aware GRPO explores coordination policies to selectively read, update, and consolidate that state during long-horizon interaction. Instantiated on ALFWorld with a Qwen3-8B LLM, EvoHarness-RL reaches 96.9% success and reveals two key dynamics: harness annealing, where training internalizes recurring harness-use patterns into the model policy and shifts the agent from frequent harness calls toward selective external-state access, and harness evolution, where progress updates and experience consolidation refine the harness into a compact, task-adaptive state substrate. These results suggest that long-horizon agents benefit from trainable policies for constructing and coordinating with external harness workspaces, beyond simply adding stronger tools or larger memories.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：长程大语言模型（LLM）智能体在复杂交互任务中需要依赖外部执行支持（如记忆、工具、状态追踪器、经验库等）来维持状态和重用经验。然而，现有方法通常通过人工设计的提示词、启发式规则或领域特定约定来手动管理这些外部组件，导致两个耦合难题：  
  - **状态形成困难**：从嘈杂的交互轨迹中可靠地提取、整理对外部状态（如环境信念、子目标进度、跨回合经验）十分困难。  
  - **运行时控制低效**：缺乏智能体自主决定何时访问、写入或整合外部状态的策略，常常导致无谓的查询消耗交互预算。
- **整体含义**：论文旨在**将外部框架的使用策略从人工工程化转变为可学习的策略问题**，让智能体在训练中学会构建外部状态，并在运行时动态、成本感知地与之协调，从而提高长程任务的成功率与效率。

### 2. 论文提出的方法论
- **核心思想**：构建一个名为 **EvoHarness‑RL** 的智能体‑框架协调层，将异构的外部执行支持抽象为统一的 **BPE（Belief, Progress, Experience）** 工作空间，并赋予智能体一组紧凑的框架元动作（`track`, `commit`, `recall`, `note`）来读写该工作空间。通过两阶段训练让智能体学会**何时**以及**如何**构建和访问外部状态。
- **关键技术细节**：
  - **BPE 抽象**：
    - **Belief**（信念）：持久化的环境状态估计，如物体位置、状态、关系。
    - **Progress**（进度）：任务分解和执行状态，记录已完成/待处理的子目标。
    - **Experience**（经验）：跨回合的知识库，包括通用技能、任务特定策略、常见错误、搜索优先级等。
  - **框架元动作**：`track [object/world]`（查询信念）、`commit [subgoal]`（写入进度）、`recall [query]`（检索经验）、`note [insight]`（记录新见解）。这些动作与领域环境动作共享同一个交互步配额，迫使智能体学习成本感知的调用。
  - **环境适配器**：将领域特定信号（如 ALFWorld 的文本观察）转换为统一的 BPE 视图，内部实现可替换，但策略始终看到相同的 BPE 接口。
  - **训练流程**：
    1. **有监督框架微调（SFT）**：利用教师模型在成功轨迹上生成包含 BPE 动作的下一动作演示，微调 Qwen3‑8B，使其学会**如何**构建有用的外部状态。
    2. **成本感知的 GRPO**：从 SFT 检查点出发，采用组相对策略优化（GRPO），奖励函数同时考虑成功奖励、效率奖励（轨迹越短越优）、动作多样性（早期鼓励探索 BPE 动作，后期逐步衰减）、重复惩罚和格式惩罚。通过该阶段，智能体学会**何时**访问外部状态才值得。
  - **公式**：轨迹奖励 \(R(\tau) = R_{\text{succ}} + \lambda_{\text{eff}} R_{\text{eff}} + \lambda_{\text{div}}(u) R_{\text{div}} - \lambda_{\text{spam}} R_{\text{spam}} - \lambda_{\text{inv}} R_{\text{inv}}\)，其中成功是主信号，效率奖励仅在成功时给予；多样性奖励带有随时间衰减的系数 \(\lambda_{\text{div}}(u) = \frac{\lambda_{\text{max}}^{\text{div}}}{2}(1+\cos(\pi u / U))\)，实现从探索到坍缩的课程。  
  - **经验演化**：在 GRPO 的 epoch 边界，用法模型对缓冲的 `note` 进行整合（添加、更新、移除），实现经验库的自适应精炼。

### 3. 实验设计
- **数据集/场景**：ALFWorld 文本环境，覆盖六种家庭任务：Pick, Look, Clean, Heat, Cool, Pick2。训练用标准分割，评估包含 seen split（140 任务）和 unseen split。
- **基准对标**：
  - 前沿模型（Claude Opus 4.5、GPT‑4.1、GPT‑5）在标准 ReAct 和加装 BPE 提示下的表现。
  - 冻结的记忆/智能体方法：ExpeL、ReasoningBank、MemP、Dynamic Cheatsheet、ACE、SkillOS‑base 等。
  - 可训练方法：标准 GRPO、SkillOS、SkillRL。
  - 自身消融/变体：EvoHarness‑Base（仅推理时）、EvoHarness‑SFT（有监督微调）、EvoHarness‑RL（完整 GRPO 优化）。
- **对比方式**：主要报告各任务平均成功率，并展示各方法在 seen 和 unseen 上的表现提升。

### 4. 资源与算力
- 论文附录 C 明确给出硬件和训练配置：  
  - **GPU**：8 块 NVIDIA H200 GPU。  
  - **推理/训练框架**：vLLM（TP=4）。  
  - **总 epoch 数 / 退火周期 U**：150。  
  - **每步采样配置**：组大小 8，每 prompt 16 个回复，每步共 128 条轨迹。  
  - **模型**：Qwen3‑8B，教师/整合模型为 Claude Opus。  
  - **最大序列长度**：prompt 12288 tokens，回复 512 tokens。  
  - 训练 optimizer：AdamW，学习率 1e‑6，KL 系数 0.01。  
  - 未明确指出总训练时长（如小时数），但给出了以上可复现的超参数。

### 5. 实验数量与充分性
- **实验组数**：  
  - 主结果表（Table 1）包含 3 大类 15 行对比，覆盖前沿模型、冻结方法、可训练方法及三阶段 EvoHarness 变体。  
  - 消融实验（Table 2）分别移除 Belief、Progress、Experience 三个组件。  
  - 泛化实验（Table 3）在 unseen split 上对比 ReAct、EvoHarness‑Base、‑SFT、‑RL。  
  - 动态分析（Section 4）展示 harness 调用次数随训练步的退火曲线、经验库规模演变。  
  - 附录 A 进一步展示分动作退火和奖励曲线。  
- **充分性与公平性**：实验覆盖了强基线（前端模型、SkillOS/SkillRL）、不同模型规模、冻结与训练方法。消融实验严格在推理时进行，排除训练干扰。结果报告了 seen 和 unseen 泛化，分析维度从性能到行为演化，相对充分。公平性方面，所有小模型均基于 Qwen3‑8B，SFT 数据统一用教师模型在相同 BPE 接口下采集；GRPO 奖励中多样性系数采取课程式衰减，可防止早期就坍缩到无 harness 策略。  
- **可能的不足**：仅在一个环境（ALFWorld）上评估，未扩展到其他长程领域（如网页导航、代码工作流），因此通用性仍需验证。

### 6. 论文的主要结论与发现
- **主要结论**：  
  - EvoHarness‑RL 在 ALFWorld 上以 Qwen3‑8B 达到 96.9% 成功率，大幅超越纯 GRPO（65.6%）和 SkillRL（89.9%），并逼近 Claude Opus 4.5 水平。  
  - BPE 抽象在推理时即可为多种规模的前端模型带来显著收益（如 GPT‑5 从 60.7% 提升至 85.0%），证明外部化信念、进度和经验是通用的。  
  - 两阶段训练（SFT → GRPO）让 harness 从静态脚手架转变为成本感知的协调策略：SFT 教会“怎么写”，GRPO 教会“是否值得写”。  
- **两大动态发现**：  
  1. **Harness 退火**：训练过程中 harness 调用次数从频繁的脚手架式使用稳步下降至接近每回合 1 次，GRPO 将重复模式内化到策略中，仅在收益大于代价时才保留选择性访问。  
  2. **Harness 进化**：跨回合的经验存储通过合并冗余、淘汰无用、保留高频调用，从膨胀状态收敛为紧凑的任务自适应“经验基板”。

### 7. 优点
- **统一的状态抽象**：将种类繁多的外部支持归纳为面向策略的 BPE 三元组，既紧凑又具备强解释性，可直接复用到不同领域。
- **可学习的协调层**：首次将框架访问决策变为策略本身可训练的一部分，而不仅仅是预定的提示词或启发式。
- **两阶段训练设计巧妙**：SFT 提供语法和基础行为初始化，GRPO 引入成本与多样性奖励实现高效“工具使用退火”，避免策略直接丢弃 harness 或过度调用。
- **丰富的演化分析**：不仅报告性能，还揭示了 harness 退火与经验进化的动态过程，为理解长程智能体如何学习管理外部状态提供了深层洞察。
- **严谨的消融**：明确证实 BPE 任一组件缺失均会导致显著性能下降，三者起协同作用。

### 8. 不足与局限
- **环境单一**：仅在文本游戏 ALFWorld 上验证，未在更复杂的多模态体现代理、网页自动化或软件工程等场景中测试，BPE 抽象的普适性待进一步证明。
- **教师依赖**：SFT 数据依赖强教师模型，GRPO 的经验整合也需外部 LLM，当迁移到全新任务时可能需要重新收集教师轨迹或设计自动合成数据的方法。
- **超参数敏感**：多样性衰减周期、各奖励权重等依赖手动设定，不同领域可能需要重新调参。
- **经验整合离线**：跨回合的技能添加、更新、移除在 epoch 边界进行，无法完全在线演化，对于实时性要求极高的场景可能存在延迟。
- **未见分割泛化中 SFT 退化**：EvoHarness‑SFT 在 unseen 任务上反而不如纯提示 harness（69.4% vs 77.6%），说明仅 SFT 可能过拟合教师策略，后续 GRPO 才纠正了这一点，但该风险仍值得关注。
- **算力需求较高**：8 张 H200 GPU 训练 150 轮，对于小团队可能资源门槛较高。

（完）
